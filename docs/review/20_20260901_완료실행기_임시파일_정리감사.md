# 2026-09-01 완료 실행기·임시파일 정리 감사

최종 갱신: 2026-09-02 10:00 KST

## 1. 정리 원칙

- 실제 실행된 BAT/PS1/SH는 성공 여부와 별개로 SHA-256을 보존해 `test_done`에 이관한다.
- 여러 실험이 재사용하는 공통 runner·source는 유지한다.
- `tmp`라는 이름만으로 삭제하지 않는다. 결과보고서가 참조하거나 다음 실험 입력인 파일은 보존한다.
- 문서, 작업로그, 핸드오프는 사용자의 사전 허가 없이 이관하지 않는다.
- 삭제 가능한 임시 항목도 즉시 삭제하지 않고 `tmp/cleanup_candidates/20260901`로 옮겼다.

## 2. `test_done` 이관 완료

| 실행 | done 파일 | SHA-256 |
|---|---|---|
| P014-R3 | `run_P014R3_mod510510_parallel_staged_certificate-20260901T042740Z-done.bat` | `763670399e1e3cb83ff341bef6c1053426630ac613d9cd1bf024dbc49fc6ee4d` |
| P014-R3 | `run_p014r3_mod510510_parallel_staged_certificate-20260901T042740Z-done.ps1` | `b56a69451a8799c83cf2ae3170e78c027690b3e7f75697a34001d663cf6cfb95` |
| P018 count 준비 | `prepare_p018_prefix_primecounts-20260901T044708Z-done.sh` | `d4e8ba4881ce95f5c569fb2cdeb5cd5552a1cfbd8f91c72c4559a083ea608926` |
| P018-P0 | `run_P018P0_prefix_information_calibration-20260901T044856Z-done.bat` | `29a03a1dc05f20cfc9c81912f0dd29dc611c3f6b0a5b5f06e5ab9dd9529309ea` |
| P018-P0 | `run_p018p0_prefix_information_calibration-20260901T044856Z-done.ps1` | `923cedc65ffe143f0acd3133d92ccd38b886b2c92782943739cf2fb81ddf9c38` |
| P018-A | `run_P018A_prefix_information_probe-20260901T125849Z-done.bat` | `3402f68c47e396d42c67c5a64d156f45e60c1725f1efd559fc739e1fc1dfd437` |
| P018-A | `run_p018a_prefix_information_probe-20260901T125849Z-done.ps1` | `0a780a440c5f91c9c110ccd350bc151135fc118e849f3d7efcbd0835b38c2bf5` |

P018-A 실행 전에는 공통 `scripts/runners/run_p018_prefix_information.ps1`, P018-A BAT/PS1,
계산 source와 logging helper를 active 위치에 유지했다. 실행 확인 뒤 전용 BAT/PS1만 위 표처럼
이관했고, 공통 runner·source·contract·evidence는 재현성과 후속 설계 감사용으로 유지했다.

## 3. 삭제 후보 폴더로 이관한 항목

다음은 삭제하지 않고 `tmp/cleanup_candidates/20260901` 아래로 이동했다.

### 3.1 내용 0개인 단위시험 임시 디렉터리

- `p010b-cover-test-*` 3개
- `p012-approval-test-*` 2개
- `p012-plot-test-*` 3개
- `p012b-approval-test-*` 3개
- 무작위 `tmp*` 디렉터리 15개
- 빈 `tmp/pdfs` 1개

합계 27개이며 sandbox 외부 읽기 전용 검사에서 모두 item 0, file 0, byte 0임을 확인했다.

### 3.2 재생성 가능한 Python bytecode cache

- `ai_dev_tool/temp_prime_gap_count_algorithm/source/__pycache__`
- `scripts/common/__pycache__`
- `source/__pycache__`
- `tests/__pycache__`

이는 Python이 다시 만들 수 있는 `.pyc` cache이며 연구 결과나 source가 아니다.

### 3.3 최종 syntax 검증용 bytecode cache

고정 FGKMT Python으로 source·공통 helper 66개를 `py_compile`한 cache는 처음부터
`validation_pycache_202609011813` 아래에만 생성했다. compile exit 0을 확인했으며 source나 결과가
아니므로 같은 cleanup candidate로 분류한다.

### 3.4 sandbox 권한 실패가 남긴 단위시험 임시 디렉터리

첫 전체 unittest는 sandbox가 `TemporaryDirectory` 접근을 막아 80개 `PermissionError`를 냈고,
그 과정에서 접근 권한이 비정상인 임시 디렉터리 16개가 `tmp` 최상위에 남았다. 이는 코드
assertion 실패가 아니라 실행환경 실패다. 정확한 절대경로를 확인한 뒤 삭제하지 않고
`sandbox_permission_test_artifacts_202609011817`로 옮겼다. 이후 같은 197 tests를 sandbox 외부에서
재실행해 모두 PASS했다. 이 16개도 연구 산출물이 아니라 cleanup candidate다.

## 4. 지금 유지해야 하는 `tmp` 항목

| 경로 | 유지 이유 |
|---|---|
| `tmp/p018-prefix-primecounts` | P018-A의 frozen exact count·READY·raw evidence 입력 |
| `tmp/p013-checkpoints` | P013/P017 exact comparison과 후속 감사에 사용 |
| `tmp/p008-primecounts` | P008 결과보고서가 참조하는 exact primecount provenance |
| `tmp/prime-gap-p005` | P005 actual calibration 정본 산출물이 이 경로에 있음 |
| `tmp/prime-gap-upstream` | P005 upstream source/build provenance와 재현성 |
| `tmp/p010b-candidate-cover` | P010B toy 결과보고서가 특정 run을 참조 |
| `tmp/setup` | PARI/GP 설치·smoke-test 이력 |

특히 P018-A를 끝내기 전에 `tmp/p018-prefix-primecounts/READY.txt`를 옮기거나 삭제하면 안 된다.

## 5. 사용자 허가 뒤 검토할 항목

다음은 이번에 이동하지 않았다.

1. `ai_dev_tool/temp_work_logs/*`
   - 작업 관련 문서이므로 사용자 허가 전 이관 금지
   - 최신 몇 개는 중단 복구에 유용하고, 오래된 것은 결과보고서가 완성된 뒤 archive 후보
2. 과거 `handoff/*_HANDOFF.md`
   - 진행 이력 정본이므로 사용자 허가 전 이관 금지
3. `ai_dev_tool/temp_prime_gap_count_algorithm`의 검토용 문서·source
   - P007/P008 이론 provenance가 있어 자동 삭제 부적절
4. `run_P014_mod510510_staged_certificate.bat`와 대응 PS1
   - P014-R3에 의해 기능상 superseded됐지만 미실행 계획의 역사와 정적 참조를 먼저 감사해야 함
5. `run_P015_48h_research_queue.bat`와 대응 PS1
   - 현재 `DO_NOT_START`이나 실행되지 않은 계획 artifact라 사용자 승인 뒤 archive 여부 결정

권장안은 문서·handoff·worklog는 당장 건드리지 않고, P018-A가 끝난 뒤 P018 primecount 원시
준비 폴더를 result에 완전히 복제했는지 한 번 더 확인한 후 정리하는 것이다.

## 6. 복구 가능성

이번 정리에서는 파일을 삭제하지 않았다. `test_done`과 `tmp/cleanup_candidates/20260901`에서 원래
위치를 확인해 복구할 수 있다. cleanup candidate의 실제 삭제는 사용자 허가 뒤에만 수행한다.

## 7. 사용자 후속 조치

2026-09-01 사용자가 `tmp/cleanup_candidates/20260901`을 직접 삭제했다고 회신했다. Codex가 같은
경로의 부재를 확인했다. 이 삭제에는 quarantine된 빈 test temp·재생성 bytecode·sandbox permission
artifact가 포함됐고, P018-A 입력인 `tmp/p018-prefix-primecounts/READY.txt`는 별도 경로에 남아
있음을 확인했다.

## 8. P018-A 완료 뒤 후속 정리 — 2026-09-02

- P018-A result에 contract, validated records, primecount CSV·metadata·준비 log·원시 알고리즘
  evidence가 복사됐고 manifest 21개 hash와 saved verification issue 0을 확인했다.
- 전용 BAT/PS1은 원본 SHA-256 불변 상태로 `test_done`에 이관했다.
- 공통 P018 runner와 source는 `KEEP_ACTIVE`다.
- `tmp/p018-prefix-primecounts`는 result 복제가 완료됐지만 P018 설계 사후감사와 provenance
  대조가 끝나지 않았으므로 현재 `RETAIN`한다. 경로명만 보고 자동 삭제하지 않는다.
- 문서·handoff·worklog는 이동하지 않았다.
- P018-A 사후 targeted test의 sandbox 권한 실패가 남긴 프로젝트 내부 0-item·0-byte
  디렉터리 5개는
  `tmp/cleanup_candidates/20260902/sandbox_permission_test_artifacts_202609021006`으로
  격리했다. 시스템 `%TEMP%`의 6개 항목은 다른 프로그램과 섞일 가능성이 있어 이동·삭제하지
  않았다.
