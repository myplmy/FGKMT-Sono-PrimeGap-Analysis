# 2026-08-27 20:35 KST 세션 핸드오프

## 1. 현재 상태

`P012A_EXPERIMENT_PASS / P012A_VISUAL_QA_PASS / P012_STATISTICAL_CONTRACT_FROZEN / P012B_IMPLEMENTED / P012B_PREFLIGHT_PASS / P012B_ACTUAL_NOT_RUN / P010B_DIRECT_COVER_TOY_PASS / P010B_ACCELERATION_BLOCKED / FULL_130_TESTS_PASS`

P012-A r2의 terminal·saved full recomputation·수치 감사와 사용자 figure 시각 QA가 모두
PASS했다. 결과를 본 뒤 규칙을 바꾸지 못하도록 P012 통계 계약을 hash로 동결했다. 그 계약만
사용하는 P012-B 독립 holdout 코드와 승인형 runner가 준비됐다. 실제 `[10^9,10^10)` holdout은
아직 실행하지 않았다.

P010B에는 작은 유한 범위에서 candidate와 exact rejection witness가 모든 정수 start를 빠짐없이
덮는지 검사하는 verifier를 구현했다. toy coverage는 PASS했지만 toy generator가 정답을 먼저
전수계산하는 순환 방식이고 더 느리므로 실제 탐색 가속은 여전히 BLOCKED다.

## 2. 연구 목적·정의·비목적

- P012-B 목적: P012-A 개발범위에서 고정한 층화 조건부 null이 독립 holdout에서도 비슷한
  설명력을 보이는지 확인한다.
- holdout gap-start 범위: `[10^9,10^10)`.
- 포함 plateau: record start와 다음 record start가 모두 holdout 안에 있는 complete record
  indices `31,32,33,34`.
- 제외: 왼쪽에서 이어져 들어온 continuation과 오른쪽 끝에서 잘린 final plateau.
- 고정: bin 3종, cohort 3종, primary family max `|z|`, secondary enrichment BH, seed
  `20260827`, Monte Carlo `100,000`, forced first record 제거, variance 0의 `z=null`,
  LOW_INFORMATION 규칙.
- P010B candidate 기준은 `gap >= H`로 equality를 포함한다. rejection prime은 반드시
  `c < q < c+H`의 엄격한 창 안에 있어야 한다.
- P012의 통계 진단은 소수간격 법칙의 증명이 아니다. P010B toy PASS도 실제
  `[10^20,10^21)` 가속의 증명이 아니다.

## 3. 이번 세션의 구현·문서

### P012-A 계약 동결

- 계약: `test_plan/P012_statistical_contract_v1.json`
- SHA-256: `1c79316de685bbc40ba3c5fc49e23b1abec328bbf07dfc218e27d74e5d82000d`
- 사용자 figure QA: PASS

### P012-B 독립 holdout

- 계획: `test_plan/P012B_stratified-null-holdout.md`
- 분석: `source/recurrence_stratified_holdout.py`
- CLI: `source/recurrence_stratified_holdout_cli.py`
- 시험: `tests/test_recurrence_stratified_holdout.py`
- runner: `scripts/experiments/p012/run_p012_stratified_null_holdout.ps1`
- preflight: PASS
- expected gap starts: `404,204,977`
- validated record hash:
  `62ecb9028e77893c79a57512488544b91b628a006bb2bd306b670afa34676297`
- P006/P011 개발 table을 holdout 추론 입력으로 사용하지 않는다.
- analysis와 saved verifier가 holdout을 각각 한 번씩 독립 재계산한다.

최종 파일 SHA-256:

- analysis: `70b8b610df0c7b7ebde987a7e66d39bb551ee0cdf95340d458ca697d79208d19`
- CLI: `eff84e063ceb8474cc9ed50f07a2c29f1876dc7b66e6c8f2c1d3fbdb495585c6`
- runner: `69ab72c66ef62b7321ab59b9d08fadfea477c08950b5d1e26b6a2b09f1e92071`

### P010B direct candidate-cover gate

- 계획: `test_plan/P010B_search-acceleration.md`
- 분석: `source/candidate_cover.py`
- CLI: `source/candidate_cover_cli.py`
- 시험: `tests/test_candidate_cover.py`
- reusable synthetic runner:
  `scripts/experiments/p010b/run_p010b_candidate_cover_toy.ps1`
- final local run:
  `tmp/p010b-candidate-cover/20260827T112421Z_p010b_candidate_cover_toy`
- final local log:
  `tmp/p010b-candidate-cover/run_20260827T112421Z_p010b_candidate_cover_toy.log`
- manifest SHA-256:
  `8d9424975e6c63d642f3c107a05bbf68b7002340e9e3a77505755b0f6653135e`
- analysis SHA-256:
  `fe9a5eeea97f35292b52e41576a3463200fadd12d3d07c9ee3da4ef1de58b7f0`

toy `[1000,10000)`, `H=20` 결과:

| 항목 | 값 |
|---|---:|
| 전체 integer starts | 9,000 |
| exact dangerous starts | 69 |
| candidate starts | 69 |
| exact rejection witnesses | 8,931 |
| factor witnesses | 7,939 |
| strict window-prime witnesses | 992 |
| coverage/soundness issues | 0 |
| ledger | 543,559 bytes |
| measured toy speedup | 0.311085 |
| acceleration | BLOCKED |

정수 필드는 bool·float·비정규 decimal을 거부하며, disk cap은 50 GiB가 아닌 십진
`50,000,000,000` bytes로 고정했다.

### 정본·결과 문서

- 영향도: `docs/method/20260827_P012B_P010B_implementation_impact_analysis.md`
- 로컬검증 보고서: `test_result/202608271750_P012B_P010B_local_validation.md`
- P012-A 결과: `test_result/202608271655_P012A_r2_result_analysis.md`
- 결과 색인: `test_result/00_실험결과_분석보고서_색인.md`
- `AGENTS.md`, `docs/METHODS.md`, 관련 theory·계획 문서를 현재 상태로 갱신했다.

## 4. 아직 하지 않은 일

- P012-B actual `[10^9,10^10)` 계산: 미실행
- P012-B 결과 통계 감사·결과보고서: actual 전이므로 없음
- P012-B 새 figure 사용자 시각 QA: figure가 아직 생성되지 않아 미실행
- P010B actual `[10^20,10^21)` candidate generator와 search: 미구현·미실행
- compressed large-range coverage ledger: 미구현
- large-prime PARI certificate binding과 survivor `prime-gap` adapter: 미구현
- baseline 대비 total-cost 개선: 미검증
- commit, push, PR: 수행하지 않음

## 5. 로컬 검증 증거

고정 Python:

```text
W:\miniforge3\envs\FGKMT\python.exe 3.11.16
```

최종 판정:

- 새 Python `py_compile`: PASS
- P012-B tests: 5/5 PASS
- P010B tests: 9/9 PASS
- 전체 unittest: 130/130 PASS, 6.150초
- active PowerShell parser: 7/7 PASS
- P012-B preflight: PASS, `holdout_prime_stream_read=false`
- P012-B confirmation 누락: exit 1, result/log 증가 0
- P010B toy confirmation 누락: exit 1
- P010B final toy·saved verification: terminal PASS, issue 0
- `git diff --check`: issue 0

sandbox 안의 TEMP ACL 때문에 P012-B 임시폴더 시험과 P010B toy runner가 각각 한 번 환경
실패했다. 동일 명령을 정상 Windows 권한에서 재실행해 모두 PASS했다. P010B sandbox 실패
run `20260827T111058Z`와 final PASS run `20260827T112421Z`를 혼동하지 않는다.

## 6. provenance·coverage·외부 취득

- 외부 다운로드: 없음
- 새 raw/validated dataset: 없음
- canonical validated record commit:
  `1a112a1387052d9ad360686313f501c01fe46b68`
- P012-B holdout endpoint `10^10`은 source exhaustive limit `10^20` 아래다.
- exact prime-count 차이는 pinned 값
  `pi(10^10)-pi(10^9)=455,052,511-50,847,534=404,204,977`을 사용한다.

## 7. 위험·열린 문제

1. P012-B에는 complete plateau가 4개뿐이라 PASS하더라도 증거력은 제한적일 수 있다.
2. 세 cohort는 모든 selected start가 `10^9` 이상이어서 같은 집합이 된다. 독립 확인이
   세 배로 늘어나는 것이 아니다.
3. holdout 결과가 개발 결과와 다르더라도 코드나 규칙을 사후 변경하면 안 된다. 차이는
   그대로 보고하고 후속 P012-C가 필요하면 새 계약을 만든다.
4. P010A의 count upper bound는 위치 목록이 아니다. absolute candidate cover 없이 실제
   탐색 범위를 안전하게 줄일 수 없다.
5. P010B actual에는 non-circular generator, 압축 coverage의 누락 없음, exact large-prime
   rejection, boundary closure, false negative 0, 같은 CPU에서 최소 5회 total-cost 비교가
   모두 필요하다.
6. P010B toy speedup은 1회 측정이고 알고리즘 benchmark가 아니다. 현재 0.311085는 단지
   순환 fixture가 더 느렸다는 진단이다.

## 8. 사용자에게 필요한 일

### A. P012-B actual 실행

- 환경: Windows PowerShell 또는 Windows 명령 프롬프트
- 시작 경로: `Z:\FGKMT-Sono-PrimeGap-Analysis`
- Conda 활성화: 필요 없음. runner가 고정 FGKMT Python 절대경로를 사용한다.
- 예상시간: 보수적으로 30–120분
- 중단 기준: 4시간 초과, RAM 8 GB 초과, disk 2 GB 초과, `[FAIL]` 출력
- GPU: 사용하지 않음

명령 프롬프트에서는 다음 두 줄을 실행한다.

```bat
cd /d Z:\FGKMT-Sono-PrimeGap-Analysis
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\experiments\p012\run_p012_stratified_null_holdout.ps1 -ConfirmP012B
```

PowerShell에서는 다음을 실행해도 된다.

```powershell
Set-Location -LiteralPath 'Z:\FGKMT-Sono-PrimeGap-Analysis'
powershell -NoProfile -ExecutionPolicy Bypass -File .\scripts\experiments\p012\run_p012_stratified_null_holdout.ps1 -ConfirmP012B
```

정상 종료 시 마지막에 다음 문구가 있어야 한다.

```text
[PASS] P012-B independent stratified-null holdout completed.
```

사용자 회신 항목:

1. 마지막 `[PASS]` 또는 첫 `[FAIL]` 문구
2. 출력된 `[RUN] result_directory=...`
3. 출력된 `[RUN] log=...`
4. 실패했다면 traceback을 포함한 로그 경로

실행이 PASS하면 Codex가 먼저 저장 산출물·통계를 감사한다. 그 뒤 사용자는 결과 폴더의
`figures/p012b_holdout_expected.png`와 `figures/p012b_holdout_residuals.png` 두 그림의 축,
범례, 글자 겹침, 잘림 여부를 눈으로 확인한다.

### B. P010B

현재 사용자 수행절차는 **별도 수행절차 필요없음**이다. 실제 가속 runner를 아직 만드는 것은
안전하지 않다. 다음 Codex 작업에서 compressed coverage schema와 exact witness adapter를 먼저
구현·검증한다.

## 9. 다음 권장 작업 순서

| 순서 | 작업 | 권장 근거 | 예상시간 | 사용자 절차 |
|---:|---|---|---:|---|
| 1 | P012-B actual 실행 | 계약이 이미 동결됐고 독립 holdout을 열 준비가 끝남 | 사용자 30–120분 | 위 Windows 명령 실행 |
| 2 | P012-B 결과·saved full recomputation 감사 | PASS 문구만으로 통계 정합성을 확정할 수 없음 | Codex 1–2시간 | 별도 수행절차 필요없음; 로그·run 경로 회신 |
| 3 | P012-B figure 시각 QA | 자동 hash·decode 검사는 축·범례의 사람 눈 판정을 대체하지 못함 | 사용자 2–5분 | 감사 후 안내할 PNG 2개 확인 |
| 4 | P010B 압축 coverage ledger schema·toy verifier | absolute 위치를 빠짐없이 덮는 형식이 실제 가속의 첫 선결조건 | Codex 2–6시간 | 별도 수행절차 필요없음 |
| 5 | P010B PARI exact witness·survivor adapter toy/중간정수 | composite·window-prime rejection을 큰 정수에서도 증명 가능한 형태로 결박 | Codex 2–4시간 | 필요 시 WSL toy 명령을 후속 제공 |
| 6 | non-circular P010B bounded pilot 설계 | 앞 gate가 모두 PASS한 뒤에만 false-negative·총비용을 측정할 가치가 있음 | 미정 | 현재 실행하지 않음 |

현재 code-ready이며 과학적으로 필요한 3–12시간 작업은 없다. P012-B는 보수적으로도 2시간
이내를 예상하며, P010B는 장시간 계산보다 증명 가능한 coverage 설계가 먼저다. 연산시간을
채우기 위한 중복 runner는 만들지 않는다.

## 10. 다음 세션의 첫 명령

사용자가 P012-B를 실행해 결과를 회신했다면:

```powershell
Get-Content -LiteralPath '<사용자가 회신한 P012-B log 절대경로>' -Tail 120
```

그 뒤 terminal PASS, manifest, contract/source hash, exact gap-start count, selected records,
saved full recomputation을 순서대로 감사한다. 결과를 보기 전에 동결한 계약은 변경하지 않는다.

사용자 회신이 아직 없다면 실제 계산을 대신 실행하지 말고 P010B compressed coverage schema의
정적 설계부터 진행한다.

## 11. 한국어 커밋 메시지 제안

제목:

```text
P012-B 독립 holdout 구현 및 P010B candidate-cover gate 추가
```

본문:

```text
- P012-A figure 사용자 QA를 PASS로 기록하고 통계 계약을 SHA-256으로 동결
- [10^9,10^10) range-only sieve와 complete record 31–34를 사용하는 P012-B 구현
- 개발 P006/P011 입력을 배제하고 두 full recomputation·approval gate runner 준비
- P010B exact candidate/rejection coverage와 strict threshold verifier 구현
- 9,000-start toy에서 69 candidates와 exact 위험 start set 일치 검증
- exact integer parsing과 십진 50,000,000,000-byte disk cap 고정
- exhaustive toy generator와 total-cost 열세를 acceleration BLOCKED로 판정
- 새 코드 py_compile, 전체 130 tests, PowerShell parser와 승인 거부 검증
- METHODS·계획·이론·결과 색인·핸드오프 갱신
```

## 12. 다음 Codex 작업의 주의사항

- P012-B actual은 사용자 실행 로그를 받은 뒤에만 결과로 판정한다.
- P012-B 실행이 확인되면 runner SHA-256을 기록하고 `test_done/`에 `-done`으로 이관한다.
- P012-B figure는 사용자 확인 전 `VISUAL_QA_PASS`로 쓰지 않는다.
- holdout 결과가 예상과 달라도 frozen contract를 고치지 않는다.
- P010B direct toy PASS를 actual acceleration으로 확대하지 않는다.
- P010B actual runner는 non-circular absolute candidate generator와 compressed exact coverage가
  준비되기 전에는 작성하지 않는다.
- commit·push·PR은 사용자 별도 요청 전 수행하지 않는다.
