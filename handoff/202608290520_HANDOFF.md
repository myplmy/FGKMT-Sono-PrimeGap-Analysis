# 2026-08-29 05:20 KST 세션 핸드오프

## 1. 현재 상태와 승인 경계

`P013-A_R2_EXPERIMENT_PASS_USER_VISUAL_QA_PENDING / P013-B_ANALYSIS_PASS_SAVED_FULL_RECOMPUTATION_RUNNING / P014_PYTHON_LIVE_PROGRESS_LOCALLY_VERIFIED_ACTUAL_NOT_RUN / P015_DO_NOT_START`

- P013-A r2는 사용자 실행, terminal PASS, saved full recomputation PASS, manifest artifact 16/16
  hash PASS다.
- P013-B는 사용자 실행 중이다. 첫 analysis는 2026-08-29 05:08 KST PASS해 result artifacts와
  checkpoint를 만들었고, 현재 두 번째 전체 재계산을 수행 중이다.
- 05:20 KST P013-B verifier: PID `61376`, FGKMT Python, CPU 누적 736.6초, working set 약
  283.9 MiB, affinity `0xff`. 직전 5초 표본은 단일코어 환산 99.14%였다.
- P014 actual은 실행하지 않았다. Python-side live progress 기능과 toy/unit 검증만 완료했다.
- P015 queue는 P013-A/B 개별 실행과 중복되므로 이번 cycle에는 실행하지 않는다.
- commit/push/PR·외부 게시·패키지 설치는 하지 않았다.

## 2. 실험 목적·정의·비목적

P013은 P012에서 미리 고정한 log-x stratified conditional recurrence null을 새 decade에 그대로
적용한다. maximal-gap record가 생긴 뒤 다음 record 전까지 같은 크기의 gap이 다시 나타나는지를
세는 별도 start-exposure 통계다. canonical FGKMT `G(x)`의 end-prime 경계는 바꾸지 않는다.

P014는 modulus-30030 count certificate를 modulus 510510으로 exact lift하고
`8,524,288,932` constraints를 streaming 검증한 뒤, 시간 gate를 통과할 때만 bounded
cutting-plane을 시도한다. count upper bound가 낮아져도 prime-gap 위치 탐색 가속이 자동으로
증명되는 것은 아니다.

P013 non-rejection은 null이 참이거나 소수간격 구조가 없다는 증명이 아니다. P014 결과도
FGKMT/Sono 무한 정리 검증이나 `[10^20,10^21)` exhaustive search 완료가 아니다.

## 3. 이번 세션 완료 작업

### P013-A r2 결과 감사

- run: `test_result/run_20260828T071601Z_p013a_recurrence_extension_1e11_r2`
- log: `test_result/logs/run_20260828T071601Z_p013a_recurrence_extension_1e11_r2.log`
- log SHA-256: `90d593369696d606c9e265a654c061233e20069f25f896ff2ccc00e99a4e56c5`
- manifest SHA-256: `86da30eb0045704c7e955433906f58129d9b10a639451ba4b5827bd344a8dbdd`
- artifact hash: 16/16 PASS
- gap starts: `3,663,002,302`; boundary 포함 prime stream `3,663,002,303`
- complete records: 36–39, gap 382·384·394·456
- recurrence: 네 plateau 모두 0
- primary: 관측 0, 기대 `0.0778288135`, family p `1.0`
- all modeled rows: 12/12 LOW_INFORMATION
- exact large-hypergeometric components: 6, 최대 sequential draws 3, binomial 근사 없음
- analysis: 약 37분 7초; 전체 runner 약 1시간 13분 16초
- 정본 보고서: `test_result/202608290456_P013A_r2_result_analysis.md`

쉬운 해석: 같은 크기의 gap이 다시 나타나지 않았지만, 원래 기대도 0.08회보다 작아 “반복 구조가
없다”고 판별할 정보가 거의 없다. 계산은 성공했지만 강한 수론 결론은 아니다.

### P013-B 비침해 live 감사

- 시작: 2026-08-28 18:00:09 KST
- 첫 analysis elapsed: `40,073.308`초, 약 11시간 8분
- gap starts: `33,489,857,205`
- first-sweep result/checkpoint 생성: 2026-08-29 05:08 KST
- selected records: 41–49, 9 complete plateaus
- 현재 stage: `p013b-saved-full-recomputation`
- `saved_verification_report.json`: 아직 없음
- provisional 수치: recurrence 0, primary 기대 `0.0667972`, family p 1.0, 27/27
  LOW_INFORMATION. 이는 saved verifier와 terminal marker 전이므로 최종 결과로 채택하지 않는다.

### CPU·무출력 원인

- `--physical-cores 4 --logical-processors 8`은 첫 4물리/8논리 안에서만 실행하도록 하는
  **허용 범위**다. 8개 worker를 자동 생성하는 옵션이 아니다.
- P013 segmented sieve·count loop는 single Python stream이다. 따라서 한 논리프로세서가 거의
  100%이고 전체 16 logical 기준 약 6.2%, 선택한 8 logical 기준 약 12.4%가 정상 구현 상태다.
- 별도 Python 학습 PID와 P013 PID를 명령줄·환경·affinity로 구분해 측정했다.
- P013 Python은 chunk progress를 `flush=True`로 출력했지만, PowerShell helper가 장시간 stage의
  stdout/stderr를 종료 후 main log에 복사하므로 실행 중 main log가 갱신되지 않았다.

### P014 Python live progress

- 새 모듈: `source/live_progress.py`
- Python은 별도 `*.progress.jsonl`에 START, phase event, 300초 HEARTBEAT, END를 직접
  `flush+fsync`한다.
- Windows console이 있으면 `CONOUT$`에 `[LIVE]` 행을 직접 출력해 PowerShell stdout redirect를
  우회한다.
- analysis와 saved verification은 별도 progress log를 사용하고 기존 파일을 덮어쓰지 않는다.
- exact lift·seed scan·iteration·final exact·saved exact의 START/PASS phase를 기록한다.
- heartbeat는 liveness이지 전체 constraints의 완료 퍼센트는 아니다.
- .NET parent-polling 구현은 검토·시험 후 최종 변경에서 제거했다. 기존 PowerShell logging
  helper와 self-test는 Git blob 기준 원상 복원했다.
- 로컬검증 보고서: `test_result/202608290515_P014_python_live_progress_local_validation.md`

## 4. 아직 하지 않은 작업

- P013-A expected/residual figure 사용자 시각 QA
- P013-B saved full recomputation 완료·terminal marker 확인
- P013-B 최종 artifact/hash/통계 감사와 결과보고서
- P013-B figure 자동 감사 뒤 사용자 시각 QA
- P014 modulus-510510 actual scan/optimizer
- P014 true constraint-percent 계측: low-level scanner chunk callback은 아직 없음
- P013/P014 single-stream 계산의 multi-process 병렬화
- P015 queue actual; 이번 cycle에는 중복이므로 실행하지 않음
- commit/push/PR

## 5. 로컬 검증 증거

- fixed Python: `W:\miniforge3\envs\FGKMT\python.exe`
- P013-A manifest artifacts: 16/16 PASS
- P013-A 저장 plateau·row·cohort·hypergeometric 불변식: issue 0
- P013-A PNG 2개: 1620x990, decode·nonblank·hash PASS
- P013-A PDF 2개: header·EOF·hash PASS
- P014 progress targeted tests: 6/6 PASS
- 전체 repository unittest: 151/151 PASS, 7.172초
- Python py_compile: PASS
- P014 read-only preflight: target constraints `8,524,288,932`, actual=false, selected logical 8 PASS
- PowerShell parser: common helper·self-test·P014 runner 3/3 PASS
- 기존 stage logging self-test: blank stdout/stderr·synthetic exit 7 PASS
- P014 runner approval denial과 root BAT usage gate: PASS
- 첫 sandbox targeted test는 사용자 TEMP 접근 거부로 중단됐고 동일 시험의 승인된 외부 재실행은
  6/6 PASS했다. 코드 회귀와 환경 차단을 구분한다.

## 6. 데이터 source·commit·coverage·다운로드

- canonical record commit: `1a112a1387052d9ad360686313f501c01fe46b68`
- validated record SHA-256:
  `62ecb9028e77893c79a57512488544b91b628a006bb2bd306b670afa34676297`
- exhaustive limit: `10^20`; P013 endpoint `10^12`보다 큼
- P012 contract SHA-256:
  `1c79316de685bbc40ba3c5fc49e23b1abec328bbf07dfc218e27d74e5d82000d`
- P013 contract SHA-256:
  `153cc1f3cd158a31e984f7d0f32502411102ab8ae6d5b9623cd0e81dcc5795bf`
- 새 외부 다운로드 없음
- P014는 기존 P010A G4 manifest/certificate/saved-report hash에 고정돼 있다.

## 7. 위험·미해결 해석

1. P013-B analysis 산출물이 있어도 saved full recomputation과 terminal PASS 전에는
   `EXPERIMENT_PASS`가 아니다.
2. P013-B main log가 05:08 뒤 조용한 것은 현재 old capture 구조상 예상된다. PID CPU가 증가하고
   있으므로 멈춤 증거가 아니다.
3. 첫 sweep이 11시간 8분이므로 두 번째 sweep도 비슷하면 전체 약 22–25시간이 걸릴 수 있다.
   원래 12–16시간 예상은 과소추정이었다.
4. P013-A와 provisional B 모두 recurrence 기대가 0.1 미만이고 모든 row가 LOW_INFORMATION이다.
   더 큰 decade brute-force가 검정력을 자동 개선하지 않을 가능성이 높다.
5. 현재 running P013-B를 멈추고 병렬화 revision으로 재시작하면 이미 사용한 11시간을 잃는다.
   현재 run에는 변경을 적용하지 않는다.
6. Python progress heartbeat는 P014의 “살아 있음·phase”를 알려주지만 exact scan의 퍼센트는 아니다.
7. P014 bound 개선도 candidate-cover completeness와 total-cost 증명 없이는 search acceleration이 아니다.

## 8. 사용자에게 필요한 일과 정확한 수행절차

### 지금 — P013-B 계속 실행

- 환경: 현재 열려 있는 Windows FGKMT Conda Prompt
- 시작 경로: 이미 실행 중인 `Z:\FGKMT-Sono-PrimeGap-Analysis`
- 수행절차: **창을 닫거나 Ctrl+C를 누르지 않는다. P014와 P015도 지금 실행하지 않는다.**
- 별도 명령: `별도 수행절차 필요없음`
- 예상 남은 시간: 05:08 기준 약 10–13시간, 대략 2026-08-29 15:00–18:00 KST 완료 가능
- 완료 시 회신:
  1. `[PASS] p013b-saved-full-recomputation` 또는 첫 `[FAIL]`
  2. `[PASS] P013-B prospective recurrence extension completed.` 여부
  3. result directory와 log 경로

### 지금 가능 — P013-A figure 시각 QA

- 환경: Windows 파일 탐색기 또는 이미지 뷰어
- 파일:
  - `test_result/run_20260828T071601Z_p013a_recurrence_extension_1e11_r2/figures/p013a_expected.png`
  - `test_result/run_20260828T071601Z_p013a_recurrence_extension_1e11_r2/figures/p013a_residuals.png`
- 예상 시간: 2–5분
- 확인: 축·범례·글자 겹침·marker·빈 그림 여부
- 회신: 두 그림이 문제없는지, 문제가 있으면 어느 그림의 어느 부분인지

### P013-B 종료 뒤 — P014 개별 실행

- 조건: P013-B terminal PASS/FAIL이 출력되고 P013-B Python process가 종료된 뒤
- 환경: Windows FGKMT Conda Prompt (`cmd.exe`)
- 시작 경로: `Z:\FGKMT-Sono-PrimeGap-Analysis`
- 명령:

```bat
cd /d Z:\FGKMT-Sono-PrimeGap-Analysis
run_P014_mod510510_staged_certificate.bat --confirm-p014
```

- 예상 시간: 약 4–19시간; CPU only, decimal 10 GB child cap
- 진행 확인: 같은 창의 `[LIVE]` 행 또는 시작 때 표시되는
  `test_result/logs/<run-id>.p014-*.progress.jsonl`; 5분마다 heartbeat
- 산출물: P014 result/log/progress logs, exact reports, best certificate, saved verification
- 회신: terminal PASS/FAIL, result directory, main log, `scientific_outcome`
- 주의: `run_P015_48h_research_queue.bat`는 실행하지 않는다.

## 9. 다음 권장 순서와 근거

1. **P013-B saved verifier 완료 대기** — 이미 첫 sweep 11시간을 썼고 최종 재현성 gate가 필요하다.
   남은 약 10–13시간.
2. **P013-A figure QA** — 수치·파일 검사는 끝났고 사람 눈 확인만 남았다. 2–5분.
3. **P013-B 결과 감사·figure QA** — terminal 뒤 Codex가 hash·산술·통계를 확인하고 사용자가
   그림을 본다. Codex 작업 약 30–60분.
4. **P014 개별 actual** — recurrence와 독립인 count-upper-bound 질문이며 진행 heartbeat가 준비됐다.
   약 4–19시간.
5. **후속 연구방향 결정** — P013-B도 LOW_INFORMATION이면 더 큰 recurrence decade보다 검정력
   설계나 연구축 축소를 우선한다. P014가 bound를 실질적으로 낮추면 P010B coverage 연결조건을
   다시 검토한다.

## 10. 다음 세션 첫 명령

P013-B가 아직 실행 중이면 명령 없이 상태만 읽는다. 종료됐다면 먼저 다음을 확인한다.

```powershell
cd Z:\FGKMT-Sono-PrimeGap-Analysis
Get-Content -LiteralPath test_result\logs\run_20260828T090006Z_p013b_recurrence_extension_1e12.log -Tail 60
```

terminal marker와 saved report를 확인하기 전에는 P013-B 결과보고서를 확정하지 않는다.

## 11. 한국어 커밋 메시지 제안

### 제목

```text
P013-A r2 결과 확정 및 P014 Python 실시간 진행 로그 추가
```

### 본문

```text
- P013-A r2 terminal·saved full recomputation과 manifest artifact 16개를 감사하고 결과보고서 작성
- 4개 complete plateau의 recurrence 0, primary 기대 0.077829, family p 1.0과 정보 부족 한계 정리
- P013-B 첫 analysis PASS와 saved full recomputation 실행 상태·단일-stream CPU 사용 원인 기록
- P014에 Python fsync JSONL heartbeat·phase event·Windows console 우회 출력을 추가
- P014 approval gate와 non-overwrite를 유지하고 P015 queue 중복 실행 금지 상태를 문서화
- targeted 6개 및 전체 151개 unittest, py_compile, preflight, parser·logging self-test 통과

P013-B saved full recomputation은 진행 중이며 P014 actual은 실행하지 않음
```

## 12. 다음 Codex 작업 지시

최신 handoff와 결과 색인을 읽고 Git·live process 상태를 다시 확인한다. P013-B가 끝났으면 원본
log SHA-256, terminal marker, manifest, checkpoint, saved report와 artifact hashes를 감사한다.
provisional 수치를 그대로 최종 판정하지 말고 plateau·component·row·cohort와 exact large sampler
metadata를 검증한다. 자동 figure 검사 뒤 사용자 시각 QA를 요청한다.

P013-B가 계속 실행 중이면 recurrence source·runner를 패치하거나 process를 중단하지 않는다.
P014 실제 결과가 오면 main log와 두 Python progress log, exact constraint count, violation 0,
best bound, scientific outcome, resource metrics를 함께 감사한다. P015 queue는 이번 cycle에 실행하지
않는다. commit/push/PR은 사용자 별도 요청 전 수행하지 않는다.
