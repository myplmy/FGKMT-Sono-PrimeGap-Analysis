# 2026-08-29 17:17 KST 세션 핸드오프

## 1. 한 줄 상태와 승인 상태

P013-B는 terminal·saved full recomputation·artifact hash를 모두 PASS하여 `EXPERIMENT_PASS`다.
관측 recurrence는 0이고 27/27 통계행이 LOW_INFORMATION이다. P013-A/B figure 사용자 시각 QA와
P017 actual은 아직 남아 있다. P014 actual과 P015 queue는 실행하지 않았다.

## 2. ChatGPT가 이해한 연구 목적·정의·비목적

프로젝트 본체는 verified maximal-gap record로 end-bounded
`G(x)=max_{p_(n+1)<=x}(p_(n+1)-p_n)`를 복원하고 iterated natural-log FGKMT scale과 비교한다.
P013은 별도 연구축으로, maximal-gap record가 생긴 뒤 다음 record 전까지 같은 크기의 gap이
재등장하는지를 gap start 기준으로 센다.

P013-B는 `[10^11,10^12)`에서 P012의 사전 고정 stratified conditional null을 독립적으로
적용했다. P013-A/B를 결과를 본 뒤 pooling하지 않으며, non-rejection을 null 모형의 증명이나
recurrence 구조 부재로 해석하지 않는다.

## 3. 완료한 파일과 구현

- P013-B 정본 결과보고서:
  `test_result/202608291712_P013B_result_analysis.md`
- P013 계획 상태 갱신:
  `test_plan/P013_prospective_recurrence_extension.md`
- 결과 색인 갱신:
  `test_result/00_실험결과_분석보고서_색인.md`
- 현재 상태 정본 갱신: `AGENTS.md`
- 단계별 작업로그:
  `ai_dev_tool/temp_work_logs/202608291708_P013B_result_analysis_WORKLOG.md`
- 실행 완료 P013-A r2·P013-B BAT/PS1 네 개를 SHA-256 불변으로 `test_done/` 이관
- 공통 `scripts/runners/run_p013_extension.ps1`은 재사용 가능하므로 유지

## 4. 실제로 하지 않은 일

- 세 번째 `[10^11,10^12)` full prime sweep을 실행하지 않음
- P013-A/B 사후 pooling 또는 새 p-value를 만들지 않음
- P013-C, P017, P014 actual을 실행하지 않음
- P017 병렬 runner를 기존 serial P013 정본으로 승격하지 않음
- P015 48시간 queue를 실행하지 않음
- package 설치·외부 다운로드·commit·push·PR을 수행하지 않음
- figure 사용자 시각 QA를 대신 PASS 처리하지 않음

## 5. 검증 명령과 결과

고정 Python:

```text
W:\miniforge3\envs\FGKMT\python.exe
```

실행 증거:

- log의 preflight·targeted tests·analysis·saved full recomputation·terminal marker: PASS
- FAIL·traceback marker: 0
- manifest artifact SHA-256: 16/16 일치
- saved report manifest hash 결속: PASS
- checkpoint canonical payload hash 결속: PASS
- 9개 plateau `N>=M>=1`, `C=M-1`: issue 0
- gap starts `33,489,857,205`, boundary 포함 prime stream `33,489,857,206`: contract 일치
- 27개 row 전부 recurrence 0·LOW_INFORMATION
- PNG 2개 decode·nonblank·1620x990, PDF 2개 header·EOF: PASS
- P017 actual result directory·log: 0개
- 작업 시작 시 git 상태: clean
- 현재 git 상태: 이번 P013-B 보고서·문서 갱신과 P013-A/B done 이관만 uncommitted

관련 단위시험:

```powershell
& 'W:\miniforge3\envs\FGKMT\python.exe' -m unittest `
  tests.test_recurrence_sequential_extension `
  tests.test_independent_research_queue -v
```

sandbox 안에서는 임시폴더 권한 `WinError 5`로 5개가 환경 차단됐다. 동일 명령을 승인된 sandbox
외부에서 재실행해 9/9 PASS했다.

## 6. dataset 원천·commit·coverage·다운로드

- source: prime-gap-list-project canonical validated records
- commit: `1a112a1387052d9ad360686313f501c01fe46b68`
- validated SHA-256:
  `62ecb9028e77893c79a57512488544b91b628a006bb2bd306b670afa34676297`
- verified exhaustive limit: `10^20`
- P013-B range: `[10^11,10^12)`, coverage 안에 완전히 포함
- P013 contract SHA-256:
  `153cc1f3cd158a31e984f7d0f32502411102ab8ae6d5b9623cd0e81dcc5795bf`
- P013-B manifest SHA-256:
  `a4272808f4486bbfba04e6255b1693420f46392f28b2c9567fddd82076c6d760`
- log SHA-256:
  `f43be4e87aacfead90cd90d11205fcdc4d5d7e8521bf12ab78a40986c68da84e`
- 이번 세션 외부 다운로드 없음

## 7. 위험·미결정·해석 주의

- P013-B primary 결과는 관측 0, 기대 `0.066797`, family p 1.0이다. enrichment를 검출하지
  못했지만 null 모형을 채택하는 결과가 아니다.
- 9개 plateau로 늘었어도 27/27 row가 LOW_INFORMATION이었다. exact-gap equality가 큰 수에서
  너무 희소하므로 단순 decade 확대의 계산비용 대비 정보효율이 낮다.
- Poisson 계획 직관으로 최소 한 번 관측 확률 50%에는 현재 대비 약 10.4배, 95%에는 약 44.8배
  기대 정보가 필요하다. 이는 검정 결과가 아니라 P013-C 착수 여부를 거르는 거친 planning gate다.
- P013-B 전체 wall time은 약 23시간이었다. main analysis와 saved full recomputation이 각각
  약 11–12시간 걸렸다.
- P017 PASS가 나오더라도 exact equality와 speed/RAM을 감사하기 전에는 serial runner를 자동
  교체하지 않는다.
- P015는 완료된 P013-A/B를 중복 실행하므로 `DO_NOT_START`다.

## 8. 다음 사용자에게 요청할 사항

### P013-A/B figure 시각 QA

- 환경: Windows 파일 탐색기 또는 Codex 파일 미리보기
- 시작 경로:
  `Z:\FGKMT-Sono-PrimeGap-Analysis\test_result`
- 명령: 별도 수행절차 필요없음
- 확인 파일:
  - `run_20260828T071601Z_p013a_recurrence_extension_1e11_r2/figures/*.png`
  - `run_20260828T090006Z_p013b_recurrence_extension_1e12/figures/*.png`
- 예상 시간: 3–5분
- 확인 항목: 축·범례·점/막대·`z undefined` marker·글자 겹침
- 회신: 각 그림에 문제가 없는지, 있으면 파일명과 위치

### P017 권장 combined queue

- 환경: Windows CMD, PowerShell 또는 FGKMT Conda Prompt
- 시작 경로: `Z:\FGKMT-Sono-PrimeGap-Analysis`
- 명령:

```powershell
cd Z:\FGKMT-Sono-PrimeGap-Analysis
.\run_P017_16h_P013_parallel_calibration_queue.bat --confirm-after-p013b
```

- 예상 시간: 약 3.5–7.5시간; child cap 합 15시간, global hard wall 16시간
- 자원: CPU 물리 4코어·논리 8프로세스, GPU 없음, 신규 disk cap 5 decimal GB
- 예상 로그: `test_result/logs/run_<UTC>_p017_*.log`
- 예상 산출물: P017-A/B child run directory와 queue run directory
- 회신: queue·child terminal marker, log·result_directory, Task Manager peak RAM 대략값
- 주의: 아래 개별 방식과 중복 실행하지 않는다.

개별 대안:

```powershell
cd Z:\FGKMT-Sono-PrimeGap-Analysis
.\run_P017A_P013A_parallel_full_calibration.bat --confirm-after-p013b
.\run_P017B_P013B_parallel_midrange_calibration.bat --confirm-after-p013b
```

개별 방식에는 aggregate 16시간 강제종료가 없으므로 combined queue가 권장안이다.

## 9. 권장 작업 우선순위와 각 근거

1. **P013-A/B figure QA** — 과학 계산은 PASS지만 시각 산출물의 마지막 사용자 확인이 필요하다.
   사용자 3–5분; 위 절차 사용.
2. **P017 combined queue** — 이미 끝난 serial 결과와 병렬 결과의 exact equality 및 실제 속도·RAM을
   측정한다. 사용자 약 3.5–7.5시간, hard wall 16시간.
3. **P017 결과 감사·승격 판단** — exact equality와 wall-time 개선을 분리 평가한다. Codex
   30–90분; 사용자 별도 수행절차 필요없음.
4. **P014 actual revision 결정 후 실행** — P014-R2 toy를 actual에 승격할지 먼저 확인하고 한
   revision만 실행한다. 기존 runner 기준 최대 약 22시간. 지금은 실행하지 않는다.
5. **P013 expected-information/power preflight 설계** — P013-C full sweep 전에 검출가능 정보량을
   수치로 거른다. Codex 1–3시간; 사용자 별도 수행절차 필요없음.
6. **P013-C 여부 논의** — 사전 정보량 증가가 충분하지 않으면 실행하지 않는다.

## 10. 승인 후 첫 실행 명령

P013-B가 완전히 종료됐으므로 현재 승인된 다음 actual 명령은 P017 combined queue다.

```powershell
cd Z:\FGKMT-Sono-PrimeGap-Analysis
.\run_P017_16h_P013_parallel_calibration_queue.bat --confirm-after-p013b
```

## 11. 한국어 commit 제목과 본문 제안

### 제목

```text
P013-B 결과 감사와 recurrence 정보부족 판정 반영
```

### 본문

```text
- P013-B terminal·saved full recomputation·artifact 16/16 hash PASS 확인
- 9개 complete plateau에서 recurrence 0, primary 기대 0.066797, family p 1.0 분석
- 27/27 LOW_INFORMATION과 단순 decade 확대의 낮은 정보효율 기록
- checkpoint·boundary count·plateau 산술·figure 자동 QA 검증
- P013 결과보고서·계획·색인·AGENTS·핸드오프 갱신
- 실행 완료 P013-A r2·P013-B BAT/PS1을 SHA-256 불변으로 test_done 이관
- 관련 단위시험 sandbox 외부 FGKMT 환경에서 9/9 통과

P013-A/B 사후 pooling, P013-C·P017·P014 actual은 수행하지 않음
```

## 12. 다음 Codex가 사용할 짧은 이어받기 지시

이 handoff와 `test_result/202608291712_P013B_result_analysis.md`를 먼저 읽는다. 사용자가 figure QA를
회신하면 P013-A/B visual 상태를 닫는다. P017 결과가 오면 queue와 두 child의 terminal marker,
manifest, exact comparison, saved report, worker 8 참여, wall time과 peak RAM을 감사한다. P017이
PASS해도 기존 serial runner를 자동 교체하지 않고 사용자에게 승격 여부를 다시 묻는다. P013-C는
expected-information/power preflight 전에는 만들거나 실행하지 않는다.
