# 2026-09-01 18:21 KST 세션 핸드오프

## 1. 한 줄 상태와 승인 상태

P014-R3와 P018-P0는 사용자 actual, terminal marker, manifest, 저장 산출물 재계산까지
`EXPERIMENT_PASS`다. P014-R3의 과학적 결과는 상한 개선 없음이고, P018-P0는 pipeline
calibration 성공이지만 정보량 0이다. 다음 사용자 실행 후보는 동결된 P018-A 하나이며, P018-B,
P019 actual, P013-C는 아직 HOLD다.

## 2. ChatGPT가 이해한 연구 목적·정의·비목적

- 정본 maximal-gap 함수는 gap의 뒤쪽 소수 `end_prime <= x`를 쓰는 end-bounded `G(x)`다.
- `log_k`는 밑 k 로그가 아니라 자연로그를 k회 반복한 값이다.
- P014 계열은 residue-state certificate의 finite count upper bound를 낮출 수 있는지 본다.
  상한 개선만으로 large-gap 위치나 exhaustive search 가속이 나오지는 않는다.
- P018 계열은 recurrence enrichment를 검정하기 전, 비싼 범위가 충분한 기대 사건과 양의 분산을
  만드는지 관측 결과를 보지 않고 판정하는 계산가치 gate다.
- P018-A가 PASS해도 최대 결론은 `균형형 B 설계를 검토하자`다. 가설 지지, full-range 자동 실행,
  수론 정리 또는 search acceleration 증명으로 해석하지 않는다.

## 3. 완료한 실행·문서·정리

### P014-R3 actual

- log:
  `test_result/logs/run_20260901T042740Z_p014r3_mod510510_parallel_staged_certificate.log`
- result:
  `test_result/run_20260901T042740Z_p014r3_mod510510_parallel_staged_certificate`
- 분석:
  `test_result/202609011805_P014R3_result_analysis.md`
- target: modulus 510510, states 92,160, constraints 8,524,288,932, threshold 1856
- 8-worker full parallel exact scan: violation 0, minimum slack 0
- saved full serial recomputation·manifest/source/artifact hash: issue 0
- upper bound: G4와 같은 `436001550591586306`
- first 5,000-constraint LP: `Unbounded`, 새 candidate 0
- 판정: `EXPERIMENT_PASS / EXACT_FINITE / SCIENTIFIC_NO_IMPROVEMENT`
- search acceleration: 미증명
- analysis부터 saved serial 종료까지 약 845.4초, 즉 14분 5초

짧게 끝난 이유는 full 12-iteration optimization을 다 했기 때문이 아니라 첫 restricted LP에서
후보 없이 일찍 종료됐기 때문이다. 후속 actual 전에 bounded seed 조건과 toy 재현이 필요하다.

### P018 exact prime-count와 P0

- count log:
  `test_result/logs/run_20260901T044708Z_p018_prefix_primecount_prepare.log`
- count evidence:
  `tmp/p018-prefix-primecounts/20260901T044708Z_p018_prefix_primecount_prepare`
- P0 log:
  `test_result/logs/run_20260901T044856Z_p018p0_prefix_information_calibration.log`
- P0 result:
  `test_result/run_20260901T044856Z_p018p0_prefix_information_calibration`
- 분석:
  `test_result/202609011806_P018P0_result_analysis.md`
- Gourdon·Deleglise–Rivat 네 endpoint: 모두 exact 일치
- P0 exact gap starts: `2232503547`
- A frozen exact gap starts: `34570543382`
- P0 primary/verifier: 16/17 서로소 segments, 모든 정수 sufficient statistics exact equality
- saved blinded recomputation·artifact/source/evidence hash: issue 0
- forced record 제거 뒤 gap 582 conditioned count·expected·variance: 모두 0
- 판정: `EXPERIMENT_PASS / CALIBRATION_ONLY_NO_GATE`
- observed recurrence·p/q/z 저장, hypothesis test, A/B gate 판정: 수행하지 않음
- P0 elapsed: 1,969.422초, 약 32분 49초
- P0 count 단순 외삽 A 계획값: 약 8.47시간, 보장값은 아니며 hard wall 12시간 유지

### 문서·정리

- 오늘자 쉬운 종합 리뷰:
  `docs/review/19_20260901_연구진행현황_가설이론_실험종합리뷰.md`
- 완료 실행기·임시파일 정리 감사:
  `docs/review/20_20260901_완료실행기_임시파일_정리감사.md`
- ChatGPT 오류·실수·환각 원장:
  `ai_dev_tool/05_ChatGPT_오류_실수_환각_원장.md`
- 스킬·AGENTS·규약 개선 제안:
  `ai_dev_tool/06_스킬_AGENTS_작업규약_개선제안_20260901.md`
- 사후 로컬검증:
  `test_result/202609011820_P014R3_P018P0_postrun_local_validation.md`
- `AGENTS.md`, `docs/METHODS.md`, P014/P018 계획, theory index, 결과 색인을 실제 상태로 갱신
- 실행 완료 BAT/PS1/SH 5개를 SHA-256 보존해 `test_done/*-done`으로 이관
- 빈 test temp·bytecode와 이번 sandbox 실패 temp는 삭제하지 않고
  `tmp/cleanup_candidates/20260901`에 격리

## 4. 실제로 하지 않은 일

- P018-A 또는 B actual
- P019 actual, P013-C, P010B large-range acceleration, Rank 85→86 exhaustive search
- P014-R4 actual 또는 bounded-seed 새 solver 구현
- figure 생성; 두 실행에는 새 figure가 없어 시각 QA 요청도 없음
- cleanup candidate 실제 삭제
- 과거 작업로그·handoff·검토용 문서 이관 또는 삭제
- 새 skill 작성, 제안한 AGENTS 일반 규칙의 영구 반영
- commit, push, PR, issue, 외부 게시

## 5. 검증 명령과 결과

고정 Python은 `W:\miniforge3\envs\FGKMT\python.exe`다.

- P014-R3/P018-P0 artifact·manifest·saved report 독립 대조: issue 0
- 전체 unittest sandbox 첫 실행: 197개 중 80개 `PermissionError`
  - 원인: sandbox의 `TemporaryDirectory` 접근·정리 거부
  - 코드 assertion 실패로 판정하지 않음
- 동일 전체 unittest sandbox 외부 재실행: `Ran 197 tests in 34.301s`, `OK`
- source·공통 helper Python `py_compile`: 66 files, exit 0
- active PowerShell parser: 12 files, error 0
- 완료 P018 SH `bash -n`: sandbox WSL `E_ACCESSDENIED` 뒤 외부 동일 명령 exit 0
- sandbox 실패가 남긴 임시 디렉터리 16개: 삭제 없이 cleanup candidate로 격리

## 6. dataset 원천, commit, coverage, 다운로드 여부

- canonical source: Prime Gap List Project `allgaps.sql`
- pinned commit: `1a112a1387052d9ad360686313f501c01fe46b68`
- validated records SHA-256:
  `62ecb9028e77893c79a57512488544b91b628a006bb2bd306b670afa34676297`
- verified exhaustive limit: `10^20`
- 이번 Codex 감사 중 새 외부 dataset 다운로드: 없음
- P018 exact primecount 입력은 사용자가 완료한 WSL run과 `READY.txt`를 그대로 보존한다.
- P018-A 전에 WSL count 준비를 다시 실행하거나 READY를 옮기지 않는다.

## 7. 위험·미결정·해석 주의

1. P014-R3의 restricted seed LP가 unbounded인 충분한 원인은 아직 증명되지 않았다. 전체 LP가
   unbounded라고 말할 수 없다.
2. P014-R3 저장 schema 내부에 R2 experiment label이 남는 metadata debt가 있다. actual artifact를
   소급 수정하지 말고 다음 revision에서 family/schema/runner revision을 분리한다.
3. P018 dual partition은 partition·boundary 오류에는 강하지만 sieve/accumulator kernel을 공유한다.
   독립 수학 증명은 아니며 exact endpoint primecount가 보조한다.
4. P018-P0의 정보량 0은 recurrence 구조가 없다는 결론이 아니라 그 한 row로는 구분할 수 없다는 뜻이다.
5. P018-A의 8.47시간은 단순 count 외삽이다. 7–10시간 예상과 12시간 hard wall을 유지한다.
6. actual peak RAM은 저장되지 않았다. 31.5 GB Job limit 적용은 확인했지만 관측 peak와 다르다.
7. working tree에는 이번 작업과 이전 미커밋 변경이 함께 있으므로 `git add .`를 쓰면 안 된다.

## 8. 다음 사용자에게 요청할 사항

### 요청 A — P018-A 한 개만 실행

- 환경: Windows CMD 또는 PowerShell
- 시작 경로: `Z:\FGKMT-Sono-PrimeGap-Analysis`
- 조건: 다른 CPU-heavy 실험과 동시에 실행하지 않음
- WSL primecount 준비·P0 재실행: 필요 없음
- CMD 명령:

```bat
cd /d Z:\FGKMT-Sono-PrimeGap-Analysis
run_P018A_prefix_information_probe.bat --confirm-p018a-prefix
```

- PowerShell 명령:

```powershell
Set-Location 'Z:\FGKMT-Sono-PrimeGap-Analysis'
.\run_P018A_prefix_information_probe.bat --confirm-p018a-prefix
```

- 예상시간: 7–10시간
- hard wall: 12시간
- 자원: 물리 4코어·논리 8프로세서, process-tree RAM ceiling 31.5 GB, GPU 미사용
- 정상 marker: 내부 `[PASS] P018-A blinded prefix information probe completed.`와 최종
  `[PASS] P018-A prefix launcher completed.`
- 회신할 것: terminal PASS/FAIL, `[RUN] result_directory=...`, `[RUN] log=...`, 대략 실행시간,
  가능하면 관찰 peak RAM

### 요청 B — 정리 후보 실제 삭제 여부 결정

`tmp/cleanup_candidates/20260901`은 빈 test temp, 재생성 가능한 bytecode, sandbox 실패 temp만
격리한 폴더다. 삭제는 아직 하지 않았다. 검토보고서
`docs/review/20_20260901_완료실행기_임시파일_정리감사.md`를 보고 삭제를 허가할지 알려 달라.

### 요청 C — 규약 개선 적용 범위 결정

`ai_dev_tool/06_스킬_AGENTS_작업규약_개선제안_20260901.md`의 권장 순서는 다음이다.

1. 기존 `log-to-result` skill에 실험별 감사 profile을 추가
2. 완료 실행기 hash·이관을 자동 점검하는 `runner-retirement` skill 작성
3. AGENTS에 family/schema/runner revision, early-exit 예상시간, PASS/과학결과 분리 규칙 추가

이번에는 제안만 작성했다. 이 세 항목의 실제 구현·영구 규칙 반영을 승인할지 알려 달라.

과거 handoff와 worklog는 이력 가치가 있어 현재는 보존을 권장한다. 이동이 필요 없다면 별도
수행절차 필요없음이다.

## 9. 권장 작업 우선순위와 근거·예상시간

1. **P018-A 사용자 실행 — 7–10시간, hard wall 12시간**
   - P0가 실행·blinding·경계·속도 선결조건을 통과했고 frozen A exact count도 준비됐다.
   - 사용자 절차는 8절 요청 A와 같다.
2. **P018-A 결과 감사 — Codex 30–90분**
   - gate 판정 전에 dual-pass equality, saved recomputation, LOW_INFORMATION, artifact hash를 본다.
   - 사용자는 log/result를 회신하는 것 외에 별도 수행절차 필요없음.
3. **P014 bounded-seed 이론·toy — Codex 약 2–6시간**
   - modulus를 더 키우기 전에 restricted LP가 bounded가 되는 충분조건을 찾아야 한다.
   - heavy actual이 아니므로 사용자 PC 수행절차 필요없음. 구현은 별도 사용자 승인 뒤 진행한다.
4. **non-circular coverage generator 이론·toy — Codex 약 4–12시간**
   - count 상한을 실제 search acceleration으로 연결하는 핵심 선결조건이다.
   - 현재 사용자 수행절차 필요없음; actual runner는 coverage와 비용 gate 전 만들지 않는다.
5. **P018-B/P019/P013-C — HOLD**
   - A가 충분한 정보를 실제로 제공하는지 감사 전에는 계산비용 대비 가치가 불명확하다.
   - 별도 수행절차 필요없음.

## 10. 다음 승인 실행 명령

다음 actual은 WSL 준비나 P0가 아니라 아래 P018-A 한 개만 사용한다. CMD에서는:

```bat
cd /d Z:\FGKMT-Sono-PrimeGap-Analysis
run_P018A_prefix_information_probe.bat --confirm-p018a-prefix
```

PowerShell에서는:

```powershell
Set-Location 'Z:\FGKMT-Sono-PrimeGap-Analysis'
.\run_P018A_prefix_information_probe.bat --confirm-p018a-prefix
```

P014-R3는 같은 입력으로 다시 실행하지 않는다.

## 11. 한국어 commit 제목과 본문 제안

제목:

```text
P014-R3·P018-P0 결과를 확정하고 연구 상태와 오류 원장을 정비
```

본문:

```text
- P014-R3의 modulus 510510 전수 exact scan과 saved serial oracle PASS를 감사
- 85억여 constraints에서 위반 0이지만 G4 상한 유지와 solver 후보 없음으로 NO_IMPROVEMENT 기록
- P018 dual-primecount와 P0 16/17 partition·saved blinded recomputation PASS를 감사
- P0 conditioned count·기대·분산 0을 calibration-only로 해석하고 P018-A 선결조건을 확정
- 오늘자 가설·이론·실험 종합 리뷰와 결과보고서·색인을 갱신
- 완료 실행기를 hash 보존해 test_done으로 이관하고 임시항목은 삭제 없이 cleanup candidate로 격리
- ChatGPT 오류·실수·환각 원장과 스킬·AGENTS·작업규약 개선 제안을 추가
- 고정 Python 전체 197 tests, 66개 py_compile, PowerShell 12개 parser, Bash syntax 검증 통과
```

commit, push, PR은 수행하지 않았다. 커밋 시에는 이번 작업 경로를 명시적으로 골라 stage하고
`git add .` 또는 `git add -A`를 사용하지 않는다.

## 12. 다음 Codex가 사용할 짧은 이어받기 지시

먼저 이 핸드오프, `test_result/00_실험결과_분석보고서_색인.md`,
`test_result/202609011805_P014R3_result_analysis.md`,
`test_result/202609011806_P018P0_result_analysis.md`,
`docs/review/19_20260901_연구진행현황_가설이론_실험종합리뷰.md`를 읽고 Git 상태를 확인한다.
P014-R3는 재실행하지 않는다. 사용자가 P018-A를 실행했다면 frozen endpoint·gate를 바꾸지 말고
log-to-result로 terminal, exact count `34570543382`, 64/65 partition, boundary, saved blinded
recomputation, resource/disk/artifact hash를 감사한다. A 결과를 recurrence enrichment 검정으로
해석하지 않는다. A 감사 전 P018-B, P019 actual, P013-C를 생성·실행하지 않는다.
