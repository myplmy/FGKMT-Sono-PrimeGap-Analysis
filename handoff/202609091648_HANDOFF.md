# 2026-09-09 16:48 KST 세션 핸드오프

## 1. 상태·승인 경계

H1b-DEP 실제 의존감사와 유한 적용성 연결, PDF 판독 규약, X_cert 진행현황 보고 완료.
전체 X_cert는 OPEN이다. 다음은 H1b-COR1 / DEP-R01 finite correlation.

- branch main, 시작 HEAD 76f4266; 시작 시 clean.
- 이번 proof commit: 066d20d4c72edc9d483ffece2de2c205cccc6577.
- 제목: H1b-DEP 실제 증명 의존성과 PDF 원문 판독 기준 정리.
- 검토한 20개 파일만 명시 allowlist로 stage/commit했다. 이 문서와 완료 원장은 별도 마감 commit.
- 승인: 문헌·수학적 증명, 짧은 toy/회귀, PDF text/TeX/OCR 구분, 단계별 로컬 commit.
- 비승인: 새 actual prime/data 실험, threshold calculator, 임의 설치, push/PR.
- 필수 새 source·Lean/패키지·장시간 계산이 필요해지면 이유와 쉬운 절차를 요청한다.

## 2. 목표·정의·이번 결론을 쉽게 설명

[AGENTS](../AGENTS.md)와 [METHODS](../docs/METHODS.md)의 end-bounded G와 반복자연로그를 유지한다.
지금 찾는 것은 “어떤 숫자 이후에는 Sono 계수 2e-17의 부등식이 모든 큰 수에서 성립한다”는
증명된 충분한 시작점 X_cert다. 유한한 실제 데이터 확인과 전역 최소 시작점은 별개다.

완료된 NORM은 세 종류의 weight 합을 같은 단위로 맞춘 것이다.
이번 DEP는 그 뒤의 증명에 어떤 재료가 정말 필요한지 대조했다.

1. FGKMT p.99와 Maynard의 하위 증명을 따라가면 P91/P92/P94/L85/L86은 필요하지만 P95는 필요 없다.
2. P95를 증명한 것이 아니다. 일반 P6.1 전체의 미완료 의무로 보존하고 현재 actual 경로에서만 제외한다.
3. 실제 h 범위는 abs(h)<4Y/X라 C_h=4로 충분하다. point의 크기와 개수도 출판 correlation lemma의 조건 안에 든다.
4. hypergraph의 두 점 동시 포함 확률은 기존 FGKMT p.82 증명으로 연결할 수 있다.
   그러나 conditioning 뒤의 한 점 확률 상계가 먼저 필요하므로 **조건부 연결**이다.

theory 47의 X>=2 exp(10^1000)은 중간 sieve scale의 child 조건이다.
이를 최종 maximal-gap 변수의 X_cert로 바꾸거나 “그 숫자까지 소수를 계산했다”고 말하지 않는다.
전역 최소 시작점은 X_cert와 같을 필요가 없다. 유효한 X_cert는 최소 시작점의 상계다.

## 3. 완료 파일·읽을 위치

| 파일 | 읽을 부분 |
|---|---|
| [theory 48](../docs/method/theory/48_Sono_FMT_H1bDEP_actual_dependency_map.md) | §3 P95 판정, §4 source crosswalk, §5 유한 연결, §6 남은 의무 |
| [review 54](../docs/review/54_20260909_H1bDEP_Xcert_진행현황_의존성감사.md) | §§1--4 쉬운 진행현황, §§5--7 PDF·검증·다음 순서 |
| [actual DAG JSON](../docs/method/theory/data/Sono_FMT_H1bDEP_actual_dependency_v1.json) | 11개 pin, 5개 PDF 선택 페이지 유형, actual/general 분리, 12 remaining nodes |
| [검산 helper](../source/h1bdep_actual_dependency.py) | fail-closed graph/hash 검사, bounded exact tuple/residue fixture |
| [전용 tests](../tests/test_h1bdep_actual_dependency.py) | 20 tests; scope·false promotion·source hash negative checks |
| [PDF 규약 09](../ai_dev_tool/09_PDF_원문_읽기_대조_규약.md) | native/TeX/scan+OCR/scan 분류, 판독 양식 |
| [오류 원장](../ai_dev_tool/05_ChatGPT_오류_실수_환각_원장.md) | E076: metadata/경로/label 시험/버전 혼동 위험 |
| [완료 작업원장](../ai_dev_tool/work_ledgers/202609091618_H1BDEP_ACTUAL_DEPENDENCY_WORK_LEDGER-done.md) | 각 단계, 실패 교정, 재개 지점 |

AGENTS, METHODS, theory index, theory 12/14/16과 parent JSON 3개를 동기화했다.
H1b parent schema는 1.16.0, T1은 1.7.0/66행 상태를 보존한다.
기존 theory 43--47, source PDF/TeX는 hash 그대로다.

## 4. 실제로 하지 않은 일

- 소수/maximal-gap 데이터 취득·변환·sweep·통계·그래프·actual runner 실행 없음.
- 기존 datas/test_result 실제 결과의 변경 없음; 새 실험 결과 파일도 없음.
- X_cert calculator, 일반 P95/전체 P6.1 증명, only-k u, literal unfiltered weight 인증 없음.
- 새 PDF 다운로드·새 OCR·패키지/Lean 설치 없음.
- source PDF 수정·재저장·기존 handoff/완료 원장 덮어쓰기 없음.
- 외부 push/PR/게시 없음. 새 figure가 없으므로 사용자 시각 QA 요청 없음.
- 실제 거대한 k개 배열·소수 목록·W·무한곱을 생성하지 않음.

## 5. 검증 명령과 관측 결과

환경: Windows PowerShell. 시작 경로 Z:\FGKMT-Sono-PrimeGap-Analysis.
연구 Python: W:\miniforge3\envs\FGKMT\python.exe.

~~~powershell
& 'W:\miniforge3\envs\FGKMT\python.exe' -m unittest tests.test_h1bdep_actual_dependency -v
& 'W:\miniforge3\envs\FGKMT\python.exe' -m unittest tests.test_h1bdep_actual_dependency tests.test_h1bnorm_common_normalization tests.test_h1bp94g_growing_dimension tests.test_h1bp91a_unweighted_moment tests.test_h1bp92a_identity_prime_moment tests.test_h1b_maynard_constant_ledger tests.test_h1c_hypothesis1_pap_trace tests.test_h1c1a_prime_distribution_inventory tests.test_threshold_proof_obligation_ledger
& 'W:\miniforge3\envs\FGKMT\python.exe' -m unittest discover -s tests
~~~

- 전용 20/20 PASS, 0.272초.
- parent 포함 표적 113/113 PASS, 0.582초.
- 전체 503/503 PASS, 59.163초, exit 0. 승인된 정상 로컬 권한 사용; actual 실험 아님.
- helper·전용/parent tests 총 5파일 py_compile PASS.
- source PDF 5개+보조 TeX 1개+선행 theory 5개: SHA-256 총 11개 일치.
- codegree toy X=20,Y=80의 서로 다른 두 점 1,770쌍을 직접 residue 열거와 exact Fraction으로 대조.
- tuple toy k=36,37,64,128; actual k>=10^200을 입력하면 toy 생성기가 거부한다.
- commit 전 21파일 strict UTF-8·4 JSON·155 local links·새 수식 구분자 issue 0.
- handoff·완료 원장 포함 최종 22파일, 4 JSON, 165 local links, 11 source hash 검사 issue 0.
- git diff --check, git diff --cached --check 각각 단독 exit 0; staged allowlist 20개 일치.
- Windows LF/CRLF 변환 안내는 있었으나 whitespace 오류 없음.
- RAM peak는 not measured; 새 heavy experiment 없음.

첫 전용 suite는 TeX label 정의를 ref 집합에 있을 것으로 잘못 기대해 1건 실패했다.
정의와 참조를 분리해 수정 후 20/20 PASS했다. gate 완화나 실패 은폐는 없다.
이 시험과 source hash 검사는 해석적 증명 전체의 독립 검증/Lean 인증이 아니다.

## 6. 원문·데이터 provenance

원문 판독은 text-first이며 추출 가능한 텍스트가 곧 native text는 아니라는 규칙을 추가했다.
FGKMT p.99, FMT p.13, Maynard p.1546, Sono p.542의 확인 페이지는 native typeset다.
RS1962 p.69는 full-page scan+숨은 텍스트층이므로 식 (3.13) 등은 원본과 대조했다.
이번 새 OCR은 없다. Maynard TeX는 최종 출판본과 버전 동일성이 미확정인 보조자료다.

원천 DOI/arXiv·PDF/TeX 경로와 hash는 actual DAG JSON에 있다.
FGKMT p.99/PDF35와 FMT p.13/PDF13만 새로 렌더했고 기존 RS p.69 렌더도 확인했다.
임시 source PDF/TeX는 후속 의존성이 있어 RETAIN한다.

datas/source_registry.json을 현재 읽어 확인한 기존 dataset pin:
1a112a1387052d9ad360686313f501c01fe46b68 (2026-08-22 취득).
canonical source는 primegap-list-project/prime-gap-list의 allgaps.sql이고 저장된 exhaustive limit은 10^20.
이번에는 원격 최신 HEAD·raw dataset·coverage를 재검증하거나 새로 다운로드하지 않았다.
따라서 위 pin과 coverage는 기존 분석의 provenance이며 2026-09-09 최신 데이터 주장으로 쓰지 않는다.

## 7. 남은 의무·위험

현재 6묶음/12작업단위:

1. DEP-R01--R06: correlation, sigma/conditioning, local count, good P, off-tuple, main moment.
2. DEP-R07--R08: hypergraph의 전체 가정·결론과 cover/smooth remainder/동시 사건.
3. DEP-R09: numerical PAP.
4. DEP-R10: numerical two-prime UB.
5. DEP-R11: 최종 확률·계수 예산.
6. DEP-R12: auxiliary X에서 최종 변수 Z의 모든 큰 값으로 전달.

이 수는 독립 정리의 확정 개수/완료율이 아니다. T1 broad row 66개와도 일대일 대응하지 않는다.
일반 SIV-07/08/09는 HARD_BLOCKER, 전체 X_cert OPEN을 유지한다.
actual identity Hypothesis가 PAP를 대신하지 않고, P94가 별도 UB를 대신하지 않는다.
FMT 구 번호와 FGKMT 출판 번호의 mapping은 가정·결론 차이까지 읽어야 한다.
일반 hypergraph codegree가 NORM의 unconditioned point bound만으로 닫히는 것이 아니다.

## 8. 사용자에게 필요한 일

지금은 추가 자료, 새 패키지·Lean 설치, 장시간 PC 계산, figure QA 요청이 없다.
**별도 수행절차 필요없음.**

현재 읽은 공개 원문과 기존 도구로 다음 correlation 감사는 시작할 수 있다.
필수 자료·설치·실제 계산이 생기면 이유, 환경, 시작 경로, 복사할 명령, 예상시간,
정상 출력, 회신할 로그 경로를 함께 요청한다.

## 9. 다음 권장 순서·근거·예상시간·사용자 절차

공통 환경은 Codex 문헌·증명, 필요한 경우 Windows FGKMT Python이다.
시작 경로는 Z:\FGKMT-Sono-PrimeGap-Analysis.
아래는 첫 감사/초안의 계획치이며 전체 증명 완료시간·사용자 PC 가동시간 보장이 아니다.

| 순서 | 작업·근거 | 계획치 | 사용자 절차 | 예상 산출물·회신 |
|---|---|---:|---|---|
| 1 | H1b-COR1 / R01: 모든 후속 moment에 필요한 finite correlation 상수 | 1--3시간, toy 수초--분 | 별도 수행절차 필요없음 | finite multiplier/cutoff 문서 또는 정확한 미해결 입력; 현재 회신 없음 |
| 2 | R02/R04: conditioning 분모와 good P 확률 | 첫 감사·초안 2--6시간, 이후 재산정 | 현재 별도 수행절차 필요없음 | sigma 비용·failure budget; 필수 source 필요시 요청 |
| 3 | R03/R05/R06: local/off/main 사건의 공통 수치 예산 | 첫 감사 2--6시간, 증명시간 미정 | 현재 별도 수행절차 필요없음 | 복합 moment/failure 원장; 필요자료만 별도 요청 |
| 4 | R07--R12: PAP/UB·hypergraph·최종 전달 inventory 갱신 | inventory 1--3시간; 전체 해결시간 미정 | 현재 별도 수행절차 필요없음 | 수치 root 연결과 남은 source 목록 |

H1b-COR1은 현재 문헌·증명 후속 범위 안이다. 새 prime sweep이나 CPU 시간 채우기용
중복 실험을 만들지 않는다. 수치 cutoff를 더 작게 최적화하는 작업은 우선 전체의 유효한
하나를 닫을 수 있는지 확인한 뒤에 비교한다.

## 10. 선택적 사용자 검산 명령

직접 확인을 원하는 경우만 Windows PowerShell에서 실행한다.

~~~powershell
Set-Location -LiteralPath 'Z:\FGKMT-Sono-PrimeGap-Analysis'
& 'W:\miniforge3\envs\FGKMT\python.exe' -m unittest tests.test_h1bdep_actual_dependency -v
~~~

예상시간 1초 내외+Python 기동. 정상 출력 Ran 20 tests, OK.
별도 actual log/result 파일은 생성하지 않고 콘솔에만 표시한다.
실패 시 전체 콘솔 출력과 실행 명령을 회신하면 된다. 실행 의무는 없다.

## 11. 한국어 커밋 제목·본문

수행한 proof commit 066d20d:

~~~text
H1b-DEP 실제 증명 의존성과 PDF 원문 판독 기준 정리

P91·P92·P94와 하위 증명을 대조해 P95가 선택한 actual 경로에 불필요함을 확인한다.
admissible tuple, C_h=4, correlation 입력 범위와 조건부 codegree 연결을 문서화한다.
X_cert의 남은 의무를 6묶음·12작업으로 분리하고 일반 정리와 root OPEN 상태를 보존한다.
PDF의 native text·TeX·scan/OCR층 구분 규약을 추가한다.
전용 20개·표적 113개·전체 503개 시험과 11개 원문/선행 hash를 검증했다.
실제 데이터 실험·threshold calculator·push는 수행하지 않는다.
~~~

문서 마감 commit:

~~~text
H1b-DEP 작업원장과 다음 증명 단계 핸드오프 마감

P95 비필수 판정과 조건부 codegree의 한계, source 버전·PDF 유형 구분을 보존한다.
503개 전체 회귀와 실제 실패·교정 기록, 다음 H1b-COR1의 순서와 사용자 절차를 명시한다.
완료 원장에 -done 접미사를 붙이되 전체 X_cert 목표는 OPEN으로 유지한다.
~~~

마감 commit 자신의 hash를 같은 문서에 넣는 commit 순환은 만들지 않는다.
최종 두 hash는 git log -2 --oneline을 따른다.

## 12. 다음 Codex의 첫 재개점

최신 AGENTS·이 handoff·완료 원장과 theory 48 §6을 읽는다.
새 H1b-COR1 원장을 만든 뒤 FGKMT 출판 Lemma 6.1 p.91의 proof를 기준으로
FMT (6.12)--(6.18)의 실제 distinct-point correlation multiplier를 우선 감사한다.
기존 explicit prime-sum 결과/교정본에서 적합한 입력을 먼저 찾고 없는 finite 연결만 증명한다.
중복 point/collision은 따로 처리하고, sigma^(-k)·conditioned sparsity는 R02로 명시한다.
theory 43--47과 이번 48의 source pins를 바꾸지 말고 새 successor로 발전시킨다.
완료된 NORM·DEP를 다시 처음부터 반복하거나 전체 root를 완료 처리하지 않는다.
