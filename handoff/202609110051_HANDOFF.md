# 2026-09-11 00:51 KST 세션 핸드오프

## 1. 한 줄 상태와 승인 상태

Theory 55 잔여 Lean 검증에서 (55.26), (55.28), untagged Ein `103/100`을
`KERNEL_PASS`로 닫았고, (55.27)은 source 입력을 숨기지 않은
`PARTIAL_FORMALIZATION`으로 보강했다. `X_cert`와 DEP-R09--R12는 계속 OPEN이다.

사용자는 잔여 Lean 검증의 우선 마무리와 단계별 로컬 commit을 허가했다.
이 허가는 push·PR·dependency upgrade·actual prime 실험으로 확장되지 않는다.

## 2. ChatGPT가 이해한 연구 목적·정의·비목적

- 주 연구 목적은 end-bounded maximal prime gap 데이터와 FGKMT/Sono scale을
  경험적으로 비교하고, 별도 이론축에서 Sono의 고정 계수 `2e-17`이 모든 더 큰
  수에 대해 보장되는 명시적 충분 threshold `X_cert`를 구성할 수 있는지 조사하는 것이다.
- `G(x)`는 gap의 `end_prime <= x`일 때 반영되고, `log_k`는 밑이 아니라 자연로그
  k회 반복이다.
- 이번 목적은 smooth-remainder 증명의 적분·endpoint·상수 전사 오류가 후속
  threshold 작업의 기반을 파괴하지 않도록 Lean 커널로 검사하는 것이었다.
- 비목적은 (55.27)의 Rosser--Schoenfeld theorem을 증명했다고 주장하거나,
  COV2·Sono/FMT 전체·`X_cert`를 Lean으로 완료했다고 확대 해석하는 것이다.

## 3. 완료한 파일과 구현

- `lean/FGKMTSono/TheoryVerification.lean`
  - (55.26): `S_delta`·kernel 정의와 `t>0`, `t!=1`의 const-rpow/FTC 적분 항등식.
  - (55.27) child: endpoint value `h(1)=delta`, exact prime-term·finite-sum 정규화,
    `1.01624 < 21/20`, premise 이후 종단 Ein 치환.
  - (55.28): 0의 removable singularity를 continuous extension·AE equality로 처리한 두
    finite-interval comparison, improper exponential moment, `51/50` factor.
  - T55-U003: exact interval split을 통한 Ein `103/100` 상계.
  - (55.29): Ein 상계 premise를 제거하고 위 커널 정리를 내부 호출.
- `lean/verification/verification_status_v1.json`, `lean/VERIFICATION_LEDGER.md`,
  `lean/tools/generate_verification_ledger.py`, `lean/verification/formula_inventory_v1.json`:
  선언·상태·한계·source hash를 동기화했다.
- `lean/README.md`, `AGENTS.md`, `docs/METHODS.md`, theory 55, review 62:
  비전문 사용자와 다음 Codex가 현재 증거 경계를 바로 찾을 수 있게 갱신했다.
- `ai_dev_tool/05_ChatGPT_오류_실수_환각_원장.md`: API 추측, validator
  attribute 인식, sandbox unittest·임시폴더 정리 시행착오를 E085로 기록했다.
- 작업원장: `ai_dev_tool/work_ledgers/202609102352_THEORY55_REMAINING_INTEGRAL_LEAN_WORK_LEDGER-done.md`.

## 4. 실제로 하지 않은 일

- `sorry`, `admit`, project-local `axiom`을 사용하지 않았다.
- Rosser--Schoenfeld `theta(t)<1.01624t` 원정리와 그 상계를 감소 kernel에 적용하는
  Stieltjes/Abel comparison을 독립 Lean theorem으로 닫지 않았다.
- maximal-gap/prime dataset을 다운로드·읽기·변환·계산하지 않았다.
- `test_result/`, 그래프, threshold calculator, 새 실험 runner를 만들지 않았다.
- Lean/Mathlib/Python package를 설치·업그레이드하지 않았다.
- push, PR, merge를 하지 않았다.

## 5. 검증 명령과 결과

사용한 고정 환경:

```powershell
Set-Location -LiteralPath 'Z:\FGKMT-Sono-PrimeGap-Analysis\lean'
& 'C:\Users\Uranus\.elan\bin\lake.exe' env lean FGKMTSono\TheoryVerification.lean
& 'C:\Users\Uranus\.elan\bin\lake.exe' build
```

- direct Lean exit 0, 경고 0.
- Lake `Build completed successfully (8765 jobs)`.

```powershell
Set-Location -LiteralPath 'Z:\FGKMT-Sono-PrimeGap-Analysis'
& 'W:\miniforge3\envs\FGKMT\python.exe' lean\tools\generate_verification_ledger.py
& 'W:\miniforge3\envs\FGKMT\python.exe' lean\tools\validate_verification_ledger.py
& 'W:\miniforge3\envs\FGKMT\python.exe' -m unittest tests.test_h1bcov2_post_covering -v
& 'W:\miniforge3\envs\FGKMT\python.exe' -m unittest discover -s tests
```

- generator: theory 56, formula 1,005, recovery 5, `GENERATED`.
- validator: declaration 87, links 1,064, banned proof escape 0,
  `KERNEL_PASS=15`, `CONDITIONAL_KERNEL_PASS=14`, `PARTIAL_FORMALIZATION=7`, PASS.
- COV2 표적 test 19/19 PASS.
- 전체 test 664/664 PASS, 74.349초. 정상 로컬 권한 최종 실행이 유효 증거다.
- 첫 sandbox 실행의 82 `PermissionError`는 코드 회귀가 아니므로 무효화했고,
  같은 suite를 정상 권한에서 재실행했다. 해당 실패가 workspace `tmp`에 남긴
  확인된 임시폴더 16개도 절대경로 점검 후 제거했다.
- `py_compile`, strict UTF-8/control-character 12파일, JSON parse, `git diff --check` PASS.

## 6. dataset 원천·commit·coverage·다운로드 여부

- 새 dataset/source 다운로드: 없음.
- raw/validated dataset 변경: 없음.
- maximal-gap coverage·source commit 변경: 없음.
- 이번 증명은 repository-local theory, 고정 Lean/Mathlib source, 기존 source record만
  사용했다.

## 7. 위험·미결정·해석 주의

- (55.26), (55.28), Ein `103/100`은 표시한 finite-real 명제의 `KERNEL_PASS`다.
- (55.27)은 `PARTIAL_FORMALIZATION`이다. 필요한 `1.01624` theta theorem은 고정
  Mathlib에 없고, 즉시 사용 가능한 `theta <= log(4)t`는 상수가 더 크다.
- (55.29)는 Ein premise는 제거됐지만 Stieltjes premise가 남아
  `CONDITIONAL_KERNEL_PASS`다.
- COV2 project 판정 `DEP-R08 ACTUAL_INPUTS_PARAMETERIZED_EXPLICIT`은 유지된다.
  이번 Lean 보강으로 broad `SIV-07/08/09`, R09--R12, `X_cert`를 올리지 않는다.
- 전수 원장 1,005개 수식 중 미형식화 항목이 많다. 현재 PASS 개수는
  전체 Sono/FMT 증명의 완료율이 아니다.

## 8. 다음 사용자에게 요청할 사항

현재 **별도 수행절차 필요없음**. 사용자가 실행할 runner, 설치할 package,
제공할 데이터가 없다.

DEP-R09 문헌 추적 중 현재 저장소에 없는 폐쇄·유료 원문이나 새 Lean/Python
dependency가 정말 필요해지면 Codex가 이유·대안·설치법을 먼저 설명하고 중단해야 한다.

## 9. 권장 작업 우선순위와 근거

1. **DEP-R09 numerical PAP source·constant·finite-range 감사**
   - 내용: Sono의 `C_PAP=1-e^-2`, `D_PAP=160`, actual interval, exceptional modulus,
     finite cutoff을 equation-by-equation으로 연결한다.
   - 근거: `X_cert` critical path에서 다음으로 열린 root이다. 55.27의 완전
     source 형식화보다 threshold 가능성을 먼저 판별한다.
   - 예상: 로컬 원문이 충분하면 2--6시간의 문헌·수식 감사. actual prime 계산 없음.
2. **DEP-R10 two-prime upper-bound sieve 명시화**
   - 내용: 두 소수 동시 조건의 uniform upper bound와 exceptional-prime 처리를 유한
     상수로 닫는다.
   - 근거: PAP와 함께 R11 전체 coefficient budget의 핵심 입력이다.
   - 예상: 4--12시간 이상; source 상태에 따라 작은 증명 단위로 분할.
3. **선택적 (55.27) Rosser--Schoenfeld·Stieltjes 완전 형식화**
   - 내용: explicit theta theorem의 analytic tail과 finite verification, 감소 kernel의
     Stieltjes/Abel lemma를 Lean으로 증명한다.
   - 근거: 증명 안전성은 더 높지만 R09 폐쇄보다 직접적인 threshold 효과가
     낮을 수 있어 3순위다.
   - 예상: 수일 이상의 별도 형식화·finite certificate 작업이 될 수 있음.
4. **DEP-R11 -> DEP-R12**
   - 내용: 같은 Sono 계수 안의 총 오차 예산을 고정하고, 보조변수에서 모든
     최종 `X`로 transfer한다.
   - 근거: R09·R10 전에 숫자를 넣으면 거짓 정밀도가 생긴다.
   - 예상: 선행 두 항의 결과 후 산정; 아직 장시간 runner 단계가 아님.

## 10. 승인 후 첫 실행 명령

다음 우선순위는 Codex의 문헌·수식 감사이므로 사용자 명령은 **별도 수행절차
필요없음**이다. 다음 Codex는 저장소 루트에서 최신 handoff와 완료 Theory 55
작업원장을 읽고, DEP-R09 전용 새 작업원장을 먼저 만든다.

이번 Lean 결과만 수동 재확인하고 싶을 때의 선택 명령은 다음과 같다.

```powershell
Set-Location -LiteralPath 'Z:\FGKMT-Sono-PrimeGap-Analysis\lean'
& 'C:\Users\Uranus\.elan\bin\lake.exe' build
& 'W:\miniforge3\envs\FGKMT\python.exe' tools\validate_verification_ledger.py
```

예상 시간은 cache 상태에 따라 약 1--3분이며, 새 `test_result`는 생성하지 않는다.

## 11. 한국어 commit 제목과 본문 제안

제목:

```text
Theory 55 잔여 적분식을 Lean 커널로 검증
```

본문:

```text
- 55.26 const-rpow 적분 항등식과 55.28 finite-interval 비교를 형식화한다
- Ein 103/100 상계를 독립 커널 정리로 닫고 55.29에 직접 연결한다
- 55.27의 finite child와 Rosser–Schoenfeld/Stieltjes source blocker를 분리한다
- 전수 원장·METHODS·review·오류 원장을 실제 증거 수준에 맞춘다
- Lake 8765 jobs, COV2 19 tests, 전체 664 tests, proof escape 0건을 확인한다
- DEP-R09~R12와 X_cert는 OPEN으로 유지한다
```

## 12. 다음 Codex가 사용할 짧은 이어받기 지시

`202609110051_HANDOFF.md`와
`202609102352_THEORY55_REMAINING_INTEGRAL_LEAN_WORK_LEDGER-done.md`를 먼저 읽어라.
(55.26), (55.28), Ein `103/100`은 `KERNEL_PASS`, (55.27)은
`PARTIAL_FORMALIZATION`이다. Rosser--Schoenfeld theta/Stieltjes를 가정 없이 닫은 것처럼
쓰지 마라. 다음 root는 DEP-R09 numerical PAP다. 새 원문·dependency·장시간 계산이
필요하면 사용자에게 이유와 대안을 보고하고 중단하라. `X_cert`는 OPEN으로 유지하라.
