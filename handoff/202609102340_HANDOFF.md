# 2026-09-10 23:40 KST 세션 핸드오프

## 1. 한 줄 상태와 승인 상태

Theory 55 식 (55.24)--(55.35)의 smooth-remainder 경로를 Lean에서 초등 계산,
조건부 합성, 미형식화 source theorem으로 분해했고 전체 검증을 통과했다. 실제 prime
실험·dataset 변경·threshold 계산은 하지 않았으며 `X_cert`는 계속 `OPEN`이다.

사용자는 권장 우선순위에 따른 작업과 단계별 로컬 커밋을 승인했다. push·PR·새 actual
실험·package 변경은 승인 범위가 아니다.

## 2. ChatGPT가 이해한 연구 목적·정의·비목적

- 목적: 검증된 end-bounded maximal prime gap과 FGKMT/Sono scale을 비교하고, 별도 이론축에서
  Sono의 고정 계수 `2e-17`이 모든 더 큰 수에 대해 보장되는 명시적 충분 threshold
  `X_cert`를 구성할 수 있는지 조사한다.
- 정본 정의: `G(x)`는 gap의 `end_prime <= x`일 때 반영하며, `log_k`는 밑이 아니라 자연로그
  k회 반복이다.
- 이번 단계의 목적: COV2 smooth-remainder project proof 안의 초등 계산 오류가 후속
  `X_cert` 작업 전체로 전파되지 않도록 Lean 커널 검증 경계를 만든다.
- 비목적: 일부 downstream 대수의 Lean PASS를 Rosser--Schoenfeld source theorem의 독립
  형식증명, 전체 Sono/FMT 증명, 가장 작은 theorem threshold 확정으로 확대하지 않는다.

## 3. 완료한 파일과 구현

- `lean/FGKMTSono/TheoryVerification.lean`
  - (55.24) exact rpow normalization과 conditional Rankin transfer.
  - (55.25) source prime-harmonic estimate 이후 terminal bound.
  - (55.26), T55-U002의 scalar kernel·Ein 정의.
  - (55.28) half-tail, improper integral, `51/50` factor와 split 합성.
  - (55.29) actual parameter의 log/exp·상수 합성.
  - (55.30) `2^(-3/4) < 3/5` 독립 증명과 conditional Euler-tail terminal bound.
  - (55.31) product-log 합성.
  - (55.32), (55.34) actual hypothesis 아래 독립 log·decay 증명.
  - (55.35) Rankin/product-log premise 이후 final exponential cancellation.
- `lean/verification/verification_status_v1.json`, `lean/VERIFICATION_LEDGER.md`,
  `lean/tools/generate_verification_ledger.py`: 식별자·선언·상태·한계를 동기화했다.
- `lean/README.md`, `AGENTS.md`,
  `docs/review/62_20260910_H1bCOV2_post_covering_구간_smooth_타당성검토.md`:
  독립/조건부/부분/미형식화 경계를 사용자와 다음 Codex가 바로 찾도록 기록했다.
- `ai_dev_tool/05_ChatGPT_오류_실수_환각_원장.md`: compile-time 시행착오와 경로 오류를
  E084로 공개 기록했다.
- 작업원장:
  `ai_dev_tool/work_ledgers/202609102247_THEORY55_SMOOTH_REMAINDER_LEAN_WORK_LEDGER-done.md`.

## 4. 실제로 하지 않은 일

- maximal-gap 또는 prime dataset 다운로드·읽기·변환·계산을 하지 않았다.
- `test_result/` 산출물, 그래프, threshold calculator를 만들지 않았다.
- Lean/Mathlib/Python package를 설치·업그레이드하지 않았다.
- (55.27)의 Rosser--Schoenfeld theta + Stieltjes source theorem을 독립 형식증명하지 않았다.
- (55.28)의 두 finite-interval integral comparison 전체를 닫지 않았다.
- `sorry`, `admit`, project-local `axiom`을 사용하지 않았다.
- push, PR, merge를 하지 않았다.

## 5. 검증 명령과 결과

시작 HEAD: `133a339b51f67d6a412a6e3a091da3238467a2f3`.

```powershell
Set-Location -LiteralPath 'Z:\FGKMT-Sono-PrimeGap-Analysis\lean'
& 'C:\Users\Uranus\.elan\bin\lake.exe' env lean FGKMTSono\TheoryVerification.lean
& 'C:\Users\Uranus\.elan\bin\lake.exe' build
```

- direct Lean exit 0.
- Lake: `Build completed successfully (8765 jobs)`.

```powershell
Set-Location -LiteralPath 'Z:\FGKMT-Sono-PrimeGap-Analysis'
& 'W:\miniforge3\envs\FGKMT\python.exe' lean\tools\generate_verification_ledger.py
& 'W:\miniforge3\envs\FGKMT\python.exe' lean\tools\validate_verification_ledger.py
& 'W:\miniforge3\envs\FGKMT\python.exe' -m unittest tests.test_h1bcov2_post_covering -v
& 'W:\miniforge3\envs\FGKMT\python.exe' -m unittest discover -s tests
```

- generator: theory 56, formula 1,005, recovery 5, `GENERATED`.
- validator: declaration 66, local links 1,064, banned escape 0, `PASS`.
- COV2 targeted tests: 19/19 PASS.
- 전체 tests: 664/664 PASS, 73.268초.
- `py_compile`, strict UTF-8/control-character, 새 link target, `git diff --check`도 issue 0.

한 묶음 명령은 `lean/` cwd에서 `lean/tools/...`를 중복해 두 Python 파일 조회가 실패했다.
해당 출력은 검증으로 세지 않았고 저장소 루트에서 위 명령을 다시 PASS했다.

## 6. dataset 원천·commit·coverage·다운로드 여부

- dataset 다운로드: 없음.
- raw/validated dataset 변경: 없음.
- maximal-gap coverage 변경: 없음.
- 이번 작업은 local symbolic Lean proof와 기존 Markdown source만 사용했다.
- canonical maximal-gap source provenance와 기존 P002--P020 결과 상태는 변경하지 않았다.

## 7. 위험, 미결정, 해석 주의

- `KERNEL_PASS`: (55.32), 기존 (55.33), (55.34)의 표시 결론과 연결된 exact child.
- `CONDITIONAL_KERNEL_PASS`: (55.24), (55.25), T55-U003, (55.29)--(55.31), (55.35).
  명시 premise에서 결론까지는 커널이 검사했지만 source premise 자체의 독립 증명은 아니다.
- `PARTIAL_FORMALIZATION`: (55.26), (55.28).
- `SOURCE_THEOREM_UNFORMALIZED`: (55.27).
- 따라서 COV2 project 판정 `DEP-R08 ACTUAL_INPUTS_PARAMETERIZED_EXPLICIT`은 유지되지만
  broad `SIV-07/08/09`, R09--R12, `X_cert`는 모두 그대로 열린 상태다.
- 1,005개 inventory 중 미형식화 항목이 많다. 현재 PASS 개수는 연구 전체 완료율이 아니다.

## 8. 다음 사용자에게 요청할 사항

현재 사용자 수행절차는 **별도 수행절차 필요없음**이다. 새 설치나 장시간 연산도 필요 없다.

향후 DEP-R09 source 추적 중 현재 저장소에 없는 유료/폐쇄 원문 또는 새 라이브러리가 꼭
필요해질 때만, Codex가 필요성과 대안을 먼저 설명하고 사용자에게 요청한 뒤 중단한다.

## 9. 권장 작업 우선순위와 근거

1. **DEP-R09 numerical PAP source·constant·finite-range 감사**
   - 내용: Sono의 PAP 입력을 actual interval, exceptional modulus, 명시 상수와 cutoff로 연결한다.
   - 근거: `X_cert` critical path의 다음 열린 root이며, 현재 smooth algebra보다 최종 threshold에
     직접적인 영향이 크다.
   - 예상: source가 로컬에 충분하면 2--6시간의 문헌·수식 감사. 새 actual prime 계산 없음.
2. **DEP-R10 two-prime upper-bound sieve 명시화**
   - 내용: 두 소수 동시 조건의 uniform upper bound와 exceptional-prime 처리를 유한 상수로 닫는다.
   - 근거: PAP와 함께 R11 전체 coefficient budget의 두 핵심 입력이다.
   - 예상: 4--12시간 이상의 증명·source tracing; 선행 source 상태에 따라 분할 필요.
3. **Theory 55 잔여 Lean hardening**
   - 내용: (55.26) 적분 항등식과 (55.28) finite-interval comparison을 추가 형식화한다.
   - 근거: project proof의 안전성은 높이지만 R09보다 `X_cert` critical path 우선도는 낮다.
   - 예상: 2--8시간; (55.27) 전체 source theorem 형식화는 별도 장기 작업일 수 있다.
4. **DEP-R11 → DEP-R12**
   - 내용: 같은 Sono 계수 안의 총 budget을 고정한 뒤 보조변수에서 최종 모든 `X`로 transfer한다.
   - 근거: R09·R10 없이 먼저 숫자를 넣으면 거짓 정밀도를 만든다.
   - 예상: 선행 두 항의 결과에 따라 결정; 아직 장시간 runner를 만들 단계가 아니다.

## 10. 승인 후 첫 실행 명령

다음 작업은 Codex의 문헌·수식 감사이므로 사용자가 실행할 명령은 없다.

다음 Codex는 저장소 루트에서 최신 handoff와 완료 작업원장을 읽고 DEP-R09 전용 새 작업원장을
먼저 만든 뒤 source inventory부터 시작한다. actual 실험 명령은 만들거나 실행하지 않는다.

## 11. 한국어 commit 제목과 본문 제안

제목:

```text
Theory 55 smooth remainder 증거 경계 Lean 검증
```

본문:

```text
- 식 55.24~55.35의 초등 계산과 외부 해석 입력 경계를 분리한다
- 로그 하계·감쇠·거듭제곱 상수와 downstream 합성을 Lean으로 검증한다
- 조건부·부분·source 미형식화 상태를 전수 원장과 검토 문서에 동기화한다
- Lake 8765 jobs, COV2 19 tests, 전체 664 tests와 proof escape 0건을 확인한다
- X_cert와 DEP-R09~R12는 열린 상태로 유지한다
```

## 12. 다음 Codex가 사용할 짧은 이어받기 지시

`202609102340_HANDOFF.md`와 Theory 55 완료 작업원장을 먼저 읽어라. (55.27)/(55.28)의
미형식화 부분을 `KERNEL_PASS`로 올리지 말고, 다음 1순위 DEP-R09 numerical PAP의 actual
source·상수·cutoff inventory를 새 작업원장으로 시작하라. 새 외부 자료·설치·장시간 연산이
필수면 사용자에게 이유와 대안을 보고하고 중단하라. `X_cert`는 계속 `OPEN`으로 유지하라.
