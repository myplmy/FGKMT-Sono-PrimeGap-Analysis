# P009/P010 결과 감사·P012 설계·P010A G4 로컬검증

## 판정

`LOCALLY_VERIFIED / 111_TESTS_PASS / ACTUAL_G4_NOT_RUN`

사용자가 완료한 P009/P010 bounded queue 산출물을 사후 감사하고, P010B saved verifier의
반복 실행 결함을 교정했으며, P012 설계 초안과 P010A G4 장시간 runner를 준비했다.
Codex는 새로운 modulus-30030 cutting-plane actual 계산을 실행하지 않았다.

## 환경

- Python: `W:\miniforge3\envs\FGKMT\python.exe`
- version: 3.11.16
- SciPy: 1.17.1, HiGHS backend
- CPU only, GPU 사용 없음
- 시작 commit: `ef25c48ea20ffabc3a912604774b1af6268500a6`

## 실제 queue 산출물 재검증

- queue terminal/manifest/summary: PASS
- P010B saved scan: issue 0
- P010A exact lift: 35,224,647 constraints exact 재계산, issue 0
- P009: manifest hash 0 issue, 두 PARI certificate 재검증 PASS, composite coverage와
  `78 < 1856` boundary arithmetic issue 0

P010B saved verifier는 최초 report 생성 뒤 다시 실행할 때 자신의 report를 prerequisite
alias로 오인했다. alias 검사를 제거하고 반복 재검증 회귀시험을 추가한 뒤 PASS했다.

## 구현 검증

실행 명령:

```powershell
& 'W:\miniforge3\envs\FGKMT\python.exe' -m py_compile `
  source/finite_gap_cutting_plane.py source/finite_gap_cutting_plane_cli.py `
  source/finite_gap_separation.py source/finite_gap_replay.py `
  tests/test_finite_gap_cutting_plane.py tests/test_finite_gap_replay.py

& 'W:\miniforge3\envs\FGKMT\python.exe' -B -m unittest discover -s tests -v
```

결과:

- `py_compile`: PASS
- full unittest: 111/111 PASS, 4.984초
- cutting-plane/slack targeted tests: 9/9 PASS
- active PowerShell parser: 6/6 files PASS
- `git diff --check`: whitespace issue 0; 기존 line-ending warning만 존재

검증 항목:

- modulus-30 smallest-slack streaming과 brute force exact 순서 일치
- floating candidate의 exact integer round·mu repair 후 모든 constraint PASS
- small-modulus working-set LP가 exact certificate로 닫힘
- 승인 없음: actual CLI가 input read/output write 전에 비정상 종료
- 승인 없음: PowerShell runner가 log/result 생성 전에 비정상 종료
- 실제 exact-lift manifest를 사용한 P010A G4 preflight PASS

runner SHA-256:

```text
scripts/experiments/p010a/run_p010a_mod30030_cutting_plane_11h.ps1
6dce831b8e29d72d4a0ef2ba62ff0120c679a6bb93c1789a37f8ec98affaa8c2
```

## 환경 오류 분리

기본 sandbox의 Windows `%TEMP%`에서 targeted unittest를 처음 실행했을 때 임시 디렉터리
ACL 때문에 `PermissionError [WinError 5]`가 발생했다. 정상 Windows 권한에서 같은 고정
Python으로 재실행하자 9/9와 전체 111/111이 PASS했다. 따라서 이는 코드 regression이
아니라 sandbox TEMP 환경 오류다.

## 미실행·사용자 확인 필요

- P010A G4 modulus-30030 actual cutting-plane: NOT RUN
- P012 local nonstationary null: 사용자 Q1–Q3 답변 전 NOT IMPLEMENTED / NOT RUN
- P009 추가 block sample: internal-zero 공급법 부재로 NOT IMPLEMENTED / NOT RUN
- commit/push/PR: 미수행
