# P005 2차 helper 교정 및 P008 구현 로컬 검증

## 판정

- P005 helper 2차 교정: `LOCALLY_VERIFIED / ACTUAL CALIBRATION NOT RUN`
- P008 phase-A 준비: `LOCALLY_VERIFIED / ACTUAL P008 NOT RUN`

이 검증은 정적 parser, 합성 fixture, approval-denial, unit test만 사용했다. P005 upstream calibration, P008 `10^20` exact prime-count input, P008 pilot/full은 실행하지 않았다.

## 고정 환경

- project: `Z:\FGKMT-Sono-PrimeGap-Analysis`
- Python: `W:\miniforge3\envs\FGKMT\python.exe`
- Python compile/tests: 고정 FGKMT 환경
- WSL: Ubuntu
- installed primecount: 7.10
- GPU: 미사용

## P008 Python 검증

### py_compile

다음 파일 모두 PASS했다.

- `source/local_residue_certificate.py`
- `source/local_residue_verification.py`
- `source/local_residue_certificate_cli.py`
- `tests/test_local_residue_certificate.py`

### targeted tests

P008 7/7 PASS:

1. exact count CSV schema
2. ledger continuity/evidence
3. tight floor와 historic conservative ceil
4. approval gate, non-overwrite, saved reverify
5. resolved-small right boundary zero certificate
6. unresolved nonempty boundary zero 금지
7. full prime-count metadata의 CSV hash·thread·memory·GPU/search 계약 검증과 mismatch 사전 거부
   - 같은 test에서 phase-A full이 계획된 `x=10^20`, 네 길이의 고정 grid 외 입력을 받지 않는 계약도 함께 검증

Windows TEMP toy fixture는 실행 샌드박스가 다시 막았고, 사용자 사전 허용 범위에 따라 sandbox 밖에서 같은 명령을 재실행해 7/7 PASS를 확인했다. 첫 실패는 코드 assertion 실패가 아니라 `C:\Users\Uranus\AppData\Local\Temp`의 `PermissionError/WinError 5`였다. `apply_patch` 자체는 전 과정에서 정상 작동했다.

### 전체 repository tests

```text
Ran 68 tests in 5.222s
OK
```

기존 61 tests와 신규 P008 7 tests가 모두 통과했다.

### P008 CLI

- pilot preflight: PASS
- fixed Python: PASS
- certificate pinned SHA-256: PASS
- supplied certificate exact global audit: PASS
- full metadata의 `counts_sha256`, 두 알고리즘 일치 표기, 최대 8 threads, 30 GiB, GPU/search 미사용 계약: 사전검증 및 저장 결과 독립검증에 포함
- phase-A full의 block grid와 `count_provenance`를 고정하고 endpoint/search 증거를 허위로 섞는 입력 거부
- candidate cover 미주장: PASS
- representative sweep≠coverage ledger: PASS
- approval flag 없는 run: exit 1
- approval-denial output directory: 생성 0건

## Windows runner 검증

PowerShell parser PASS:

- `run_local_residue_certificate.ps1`
- `run_finite_gap_certificate.ps1`
- `run_plateau_recurrence.ps1`
- `scripts/powershell_stage_logging.ps1`

BAT approval-denial PASS:

- P008 pilot/full
- P006 full
- P007 full

공통 logging toy self-test도 PASS했다.

- blank stdout/stderr line 보존
- synthetic stderr 기록
- synthetic exit 7 전달
- 전체 ErrorRecord와 stack 기록
- 최종 marker `POWERSHELL_STAGE_LOGGING_SELFTEST_PASS`

## WSL/Bash 검증

`bash -n` PASS:

- `scripts/run_prime_gap_cpu_calibration.sh`
- `run_P005_prime_gap_cpu_calibration.sh`
- `prepare_P008_local_primecounts.sh`
- 두 신규 toy shell tests

failure-handler semantics toy:

```text
BASH_ERREXIT_MANIFEST_TOY_PASS
```

failure manifest writer 내부의 `set +e`가 subshell 밖 parent `errexit`를 끄지 않고, 실패 다음 명령이 실행되지 않음을 확인했다.

합성 gaps SQL import:

```text
P005_GAPS_DB_TOY_PASS rows=2
```

canonical `allgaps.sql`은 읽기 전용으로 다음을 확인했다.

- SHA-256 `988c3278d95a16460a9897e09829fb061ee854e55efa6c4fcec3b9779930894f`
- `CREATE TABLE gaps (...)`
- `INSERT INTO gaps ...` 존재

canonical raw SQL을 임시 SQLite로 실제 변환하는 검증은 실행 승인 심사에서 “actual raw dataset transformation”으로 거부됐다. 지시대로 우회하지 않고 합성 fixture로 대체했다. 따라서 canonical 15 MB import의 실제 성공은 다음 사용자 P005 calibration에서 처음 확인한다.

## primecount compatibility 검증

설치된 `primecount 7.10`은 최신 README에 있는 `--double-check` 옵션을 지원하지 않았다. P008 helper를 그 옵션에 의존하게 만들지 않고 두 exact 알고리즘을 별도 실행하도록 수정했다.

toy `pi(100)`:

```text
Gourdon = 25
Deleglise-Rivat = 25
```

두 알고리즘 결과 불일치 시 input 준비를 즉시 중단한다. 이 toy 확인만 수행했으며 `10^20` endpoint는 계산하지 않았다.

## 검증하지 않은 항목

- P005 canonical `gaps.db` 실제 import와 upstream `gap_stats`
- P005 새 end-to-end calibration
- P008 BAT pilot
- P008 `pi(10^20+L)` 5 endpoint 계산
- P008 full local bound 결과
- candidate cover, coverage ledger 실제 생성, direct speedup

위 항목은 각각 사용자 실행·승인 뒤에만 PASS/FAIL을 판정한다.
