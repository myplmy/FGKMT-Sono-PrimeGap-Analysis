# P013·P014·P015 48시간 runner 준비 로컬검증

## 판정

`LOCALLY_VERIFIED / ACTUAL_NOT_RUN / READY_FOR_USER_EXECUTION`

P013-A/B prospective recurrence extension, P014 modulus-510510 staged certificate와 P015
orchestration-only queue를 구현했다. 고정 FGKMT Python에서 정적·toy·승인 차단·전체 회귀검증은
PASS했다. Codex는 새 범위 sieve나 8,524,288,932-constraint actual scan을 실행하지 않았다.

## 구현 범위

### P013

- `[10^10,10^11)`: exact gap starts `3,663,002,302`, complete records `36–39`
- `[10^11,10^12)`: exact gap starts `33,489,857,205`, complete records `41–49`
- P012 규칙, stage별 100,000회, seed와 alpha 0.025를 새 JSON contract로 사전 동결
- range streaming 분석 뒤 saved verifier가 전체 range를 두 번째로 재계산
- observed/expected와 residual PNG·PDF 생성; variance 0은 z=0이 아닌 undefined x marker

P013 contract SHA-256:
`153cc1f3cd158a31e984f7d0f32502411102ab8ae6d5b9623cd0e81dcc5795bf`

### P014

- G4 exact certificate hash·manifest·saved report를 고정하고 기존 saved exact verifier 재확인
- `30030 | 510510`, `lambda>=0` exact lift
- target states `92,160`, target constraints `8,524,288,932`
- full matrix lower bound 약 `825.650 GiB`: materialization 금지
- chunk 64/top-k 5,000 보수적 scan memory 약 `542.441 MiB`
- signed-int64 guard `279298307186501460 < 9223372036854775807`
- stage-A exact lift scan이 4시간 gate 안일 때만 최대 12회 bounded working-set optimization
- 최종 certificate와 saved verifier에서 모든 constraint exact 재검증
- count upper bound만 연구하며 direct search acceleration은 항상 false

### P015

- 순서: P013-A 4시간 → P013-B 20시간 → P014 22시간
- P013-A shared-code failure이면 P013-B만 BLOCKED; 독립 P014는 계속
- global wall `169,200`초(47시간), aggregate output decimal 50GB
- child는 자체 run id·log·manifest·saved report를 유지
- queue는 실행·timeout·disk·exit만 기록하고 과학 결과를 합치지 않음
- child FAIL/BLOCKED가 하나라도 있으면 마지막 queue exit는 nonzero

## 로컬 검증 증거

| 검증 | 결과 |
|---|---|
| 새 Python source/test `py_compile` | PASS |
| 새 PS1 5개와 active `scripts/**/*.ps1` 전체 parser | 5/5, 11/11 PASS |
| P013-A/B preflight | PASS; extension prime stream read=false |
| P014 resource/provenance preflight | PASS; actual=false |
| PS1 approval-denial | 4/4 PASS |
| root BAT usage gate | 4/4 PASS |
| fixed-Python full unittest | 140/140 PASS, 6.379초 |
| `git diff --check` | PASS |

기본 sandbox 안의 첫 targeted test에서는 테스트 본문이 통과한 뒤 `TemporaryDirectory` 정리에서
Windows `WinError 5`가 두 번 발생했다. 동일 전체 suite를 사용자 승인 범위의 sandbox 밖에서
고정 Python으로 재실행하자 140개가 모두 PASS했다. 따라서 이는 계산 코드 회귀가 아니라
sandbox 임시폴더 권한 문제로 분리했다.

## 실행하지 않은 것과 이유

- P013-A/B actual: 수십억 gap start를 읽는 승인형 heavy 실험이므로 사용자 실행 대상
- P014 actual: 85억 개 이상 제약의 반복 scan이므로 사용자 실행 대상
- P010B large-range actual: non-circular absolute candidate mapping과 total-cost proof 미완성
- P005 Rank 85→86 exhaustive: 누락 없는 every-start coverage ledger 미완성
- modulus-30030 G4 재실행: 이미 full convergence라 새 질문 없는 중복

## 사용자 실행

Windows 명령 프롬프트 또는 FGKMT Conda Prompt에서 실행할 권장 명령은 아래 하나다.

```bat
cd /d Z:\FGKMT-Sono-PrimeGap-Analysis
run_P015_48h_research_queue.bat --confirm-48h
```

예상 합계는 약 14.5–37시간, hard global cap은 47시간이다. queue를 실행했다면 개별 P013/P014
BAT를 다시 실행하지 않는다. queue 종료 뒤 terminal PASS/FAIL, queue log/result directory,
각 child log/result directory와 P013 figure 두 장씩의 시각 상태를 회신한다.
