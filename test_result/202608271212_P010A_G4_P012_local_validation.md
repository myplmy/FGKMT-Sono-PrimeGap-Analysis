# P010A G4 결과 감사·P012-A 구현 로컬검증

## 판정

`P010A_G4_SAVED_EXACT_PASS / P012A_IMPLEMENTED / P012A_LOCALLY_VERIFIED / P012A_ACTUAL_NOT_RUN / HOLDOUT_UNTOUCHED`

## P010A G4 독립 결과 감사

- terminal marker: PASS
- manifest artifact hash mismatch: 0
- checkpoint count: 4, manifest count 4
- checkpoint payload와 `iteration_history.json`: 일치
- copied exact-lift manifest/certificate binding: 일치
- best certificate exact scan: 35,224,647 constraints, violation 0, minimum slack 0
- total upper bound: `436001550591586306`
- strict improvement flag: 정수 비교와 일치
- direct search acceleration flag: false

검증 명령은 fixed FGKMT Python에서
`source.finite_gap_cutting_plane.verify_saved_cutting_plane`을 새로 호출해 saved report와
독립적으로 full exact scan했다. 반환은 `status=PASS`, `issues=[]`,
`exact_recomputed=true`였다.

## P012-A 구현 검증

구현:

- `source/recurrence_stratified_null.py`
- `source/recurrence_stratified_null_cli.py`
- `tests/test_recurrence_stratified_null.py`
- `scripts/experiments/p012/run_p012_stratified_null_development.ps1`

검증 결과:

| 검사 | 결과 |
|---|---|
| `py_compile` 3 files | PASS |
| P012 toy tests | 4/4 PASS |
| 전체 unittest | 115/115 PASS, 6.094초 |
| active PowerShell parser | 7/7 files PASS |
| pinned P006/P011 input preflight | PASS, 29 plateaus·28 P011 rows |
| runner without `-ConfirmP012A` | expected nonzero, actual 전 log/result 미생성 |
| P012-A actual | NOT RUN |
| `[10^9,10^10]` holdout | NOT TOUCHED |

toy tests는 다음을 확인했다.

1. 모든 bin scheme에서 forced first record가 정확히 한 번 제거됨
2. component exposure 합이 P006 `N-1`과 일치
3. component recurrence 합이 P006 `C`와 일치
4. 고정 seed Monte Carlo 재현
5. primary·shifted 두 sensitivity scheme 모두 보존

## sandbox 환경 오류 분리

최초 sandbox 내부 toy unittest는 테스트 본문이 아니라
`C:\Users\Uranus\AppData\Local\Temp\tmp...` 정리에서 `PermissionError [WinError 5]`가
발생했다. 동일 FGKMT 명령을 정상 Windows 권한에서 다시 실행하자 4/4 PASS했고, 전체
115 tests도 PASS했다. 따라서 코드 회귀가 아니라 기존 sandbox TEMP ACL 환경 오류로
분류한다.

## 승인·실행 경계

Q1–Q3 설계 승인은 계획과 코드 구현에 반영했다. 실제 P012-A 통계 계산은 별도
`-ConfirmP012A` 사용자 실행으로만 시작한다. Codex는 이 검증에서 실제 `[2,10^9]`
stratified analysis나 그래프를 생성하지 않았다.

## 장시간 후보 감사

현재 code-ready이며 과학적으로 타당한 3–12시간 runner는 0개다. 장시간을 채우기 위한
중복 실행은 만들지 않았다. 근거와 후보별 gate는
`docs/method/20260827_3to12h_compute_readiness_audit.md`에 기록했다.
